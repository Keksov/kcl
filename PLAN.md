# kcl — план доработок по ревью 2026-09-06

Источник: `REVIEW.md` (находки с ID), решения владельца D1–D8 (2026-09-06), рекомендованные
умолчания R1–R15 (владелец может отменить любое до начала соответствующей фазы). Машинный журнал —
`kcl_ledger.json`. Конвенция та же, что у kklass/PLAN.md: фаза = ветка работ с гейтом; коммит
каждой фазы только по явному «go»/«коммить» владельца; сабмодуль kcl коммитится раньше kbool.

## 0. Для новой сессии: контекст, окружение, запуск

**Где что лежит.** `REVIEW.md` — сводка находок; `docs/review-2026-09-06/0N_*.md` — полные отчёты восьми ревьюеров с точными `file:line`, командами воспроизведения, «наблюдалось / ожидалось» и пробелами в тестах; `docs/review-2026-09-06/repro/gN/*.sh` — их скрипты (пути внутри могут указывать на scratchpad старой сессии — поправить `K=`/`source`); `docs/review-2026-09-06/00_BRIEF.md` — бриф ревью с контрактами kklass, по которым судили код. `kcl_ledger.json` — журнал фаз. Память сессии: `kcl-review-plan` в `~/.claude/projects/c--projects-kkbot-kbool/memory/`.

**Окружение.** Windows 11, MSYS2. Основной bash 5.2.37 = `bash` в PATH. Второй bash 5.3.9 (cygwin): `C:/bin/msys64/usr/bin/bash.exe`; наборы на нём запускать ТОЛЬКО так: `PATH="/c/bin/msys64/usr/bin:$PATH" /c/bin/msys64/usr/bin/bash.exe tests/tests.sh` — без префикса PATH дочерний `bash` раннера оказывается msys 5.2 и набор сообщает 0 тестов. `python` в PATH — заглушка Microsoft Store, зависает; использовать perl/awk/sed. Локаль по умолчанию пустая (bash в C) — см. D6.

**Запуск тестов.** Набор юнита: `bash kcl/<unit>/tests/tests.sh [--verbosity info]` (ktests; на pass при verbosity по умолчанию печатает ничего; фильтр по имени файла — аргументом). Мастер-свип: `bash kbool/tests/tests.sh` (≈8–9 мин на версию; 20 наборов). Два набора/два bash — строго последовательно (гонка на `tests/.ckk` и `/tmp`). Итог смотреть grep-ом по ВСЕМ строкам `[FAIL]`, не по хвосту. Бенчи: `bash kcl/<unit>/bench.sh` там, где есть; правило — ни одна метрика не хуже; не перечислять таблицу функций большого шелла (`compgen -A function`) — замедляет весь процесс.

**Контракты kklass (актуально после kklass `b564029`, 2026-09-05).** `func` возвращает через `kk._return V` → `$RESULT`; direct-вызов НЕ печатает, под `$()` печатает ровно один раз; вычисляемые свойства — так же (D1 kklass). Статические методы со static-свойствами теряют состояние под `$()` — значение и в `REPLY`. Инстанс = `<inst>_data` + `<inst>_class` + обёртки `<inst>.<член>()`; `.delete` освобождает только это — дополнительные массивы юнита (`${inst}_items` и т.п.) освобождает деструктор юнита. Зарезервированные имена членов: `this __inst__ __class__ RESULT REPLY IFS __kk_*`. `inherited`/`.parent` — от класса-владельца тела; `.call` виртуальный. Fork в горячем пути = 1–16 мс на большом шелле.

**Состояние рабочего дерева на момент передачи (2026-09-06).** kcl: НЕ закоммичены `REVIEW.md`, `PLAN.md`, `kcl_ledger.json`, `docs/review-2026-09-06/`, `.gitignore` (`**/.ckk`), и незавершённый thashset (`thashset/thashset.sh`, `thashset/tests/`, `thashset_ledger.json` — P1 из P0–P4, продолжается по своему PLAN после правок G2-01/02/04/05). kklass и kbool синхронны с origin/main. Коммиты — только по явному «go»/«коммить»; сабмодуль раньше родителя.

**FPC-эталон.** Паритет сверять по исходникам FPC 3.2.2: `custapp.pp`, `inifiles.pp`, `dateutil.inc`, `stringl.inc`/`classes`, `syshelph.inc` (TStringHelper), `math.pp`, `generics.collections`. Локального чекаута и установленного FPC на машине нет; исходники берутся из зеркала GitLab по тегу `release_3_2_2`, шаблон ссылки:

```
https://gitlab.com/freepascal.org/fpc/source/-/raw/release_3_2_2/<путь-в-дереве>
```

Уже использовано: `packages/fcl-base/src/custapp.pp` (784 строки) — фаза P4, порт всего парсера опций построчно.

## 1. Решения владельца (2026-09-06)

| ID | Решение |
|---|---|
| **D1** | Числовые аргументы валидируются **общим хелпером** `kk.isInt` / `kk.isNum` в kkore (`klib.sh`), с нормализацией `10#`; нарушение = rc 1, без вывода. |
| **D2** | Контракт инстанс-юнитов: ошибка = **rc 1 + RESULT="" + stderr только под одним debug-переключателем** (`VERBOSE_KKLASS=debug`); булевы ответы = **rc**. |
| **D3** | **Все шесть** «печатающих» статических юнитов (dateutils, tstringhelper, math, tfile, tpath, tdirectory) переводятся с `proc`+echo на `func`+`kk._return` (direct = RESULT без форка; `$()` продолжает печатать). |
| **D4** | tcustomapplication: **верный порт FPC 3.2.2** `CheckOptions`/`FindOptionIndex`/`GetOptionValue` (`--opt=value`, `:`/`::`, CaseSensitiveOptions, AllErrors, тексты ошибок FPC, DoRun, ParamCount/Params); тесты, закрепившие старый диалект, переписываются. |
| **D5** | Разделитель путей на MSYS/cygwin: **POSIX** — `DirectorySeparatorChar='/'`; парсеры tpath принимают на входе и `/`, и `\`. Ожидания tpath 001/003/007 меняются. |
| **D6** | Локаль: **требуется UTF-8**; раннеры тестов пинят `LC_ALL=C.UTF-8`; юниты при загрузке экспортируют `LC_CTYPE=C.UTF-8`, если `LC_ALL`/`LC_CTYPE`/`LANG` пусты; тесты с не-ASCII перед искомым символом; fpjson-лексер локально пинит `C`. |
| **D7** | `set -e` и `set -u` — **общий контракт kcl**: гварды `${_X_SOURCED:-}`, `if`/`+= 1` вместо хвостовых `&&`/`(( x++ ))`, смоук-тест `set -eu` в каждом наборе. Булевые rc-методы под `set -e` вызываются через `if`/`||` — документируется. |
| **D8** | fpjson P0: **чистый bash** — handle-модель (инстанс документа + параллельные массивы) + **оконный regex-лексер**; awk-ускоритель остаётся в P8 плана fpjson. |

## 2. Рекомендованные умолчания (действуют, если владелец не отменит)

| ID | Умолчание | Находка |
|---|---|---|
| R1 | `TList.Add` возвращает **индекс** (FPC, docs, TStringList уже так) | G1-06 |
| R2 | `capacity < count` и `capacity < 0` → rc 1, состояние не меняется (FPC EListError); закрывает и утечку в TObjectList | G1-09, G1-07b |
| R3 | Отвергнутый токен конструктора (`TObjectQueue.new q bogus`, `TObjectDictionary.new d bogus`): инстанс создаётся **с умолчаниями** + rc 1 — одинаково в обоих юнитах, задокументировано | G2-10 |
| R4 | `AddOrSetValue k sameOwnedHandle` освобождает handle (FPC-паритет) + предупреждение в README | G2 Q2 |
| R5 | `Assign` проверяет класс операнда через `${src}_class` (тот же класс или потомок), не только наличие `_items` | G2-04, G2 Q3 |
| R6 | tinifile: **гибридная** валидация (отклонять на записи то, что FPC-ридер переинтерпретирует; loader verbatim); `SectionExists` = FPC (`not Empty`); persistence: `-d`/`! -w` гварды + `--` везде + tmp+mv; индекс поиска — вариант (a) (убрать вызов функции на строку и полное сканирование), (b) только если появится потребитель с >500 ключами; `\` в пути нормализуется для расчёта каталога | T1–T8, T12 |
| R7 | dateutils: локальный offset **на дату** через `printf '%(%z)T' <epoch>` (без форка); `now`/`today` без изменений | G3-05 |
| R8 | Булевы функции конвертируемых статиков: **rc** + `kk._return true|false` (совместимость с `$()`-вызовами) | X-CONTRACT |
| R9 | tstringhelper: `replace` по умолчанию **все** вхождения (FPC); `indexOf('')` = -1 (FPC); `toBoolean` = FPC TryStrToBool; `split` → выходной массив по nameref + count в RESULT; `trim` режет `<= #32` | TSH-05, 07, 12, 13, 03 |
| R10 | tcustomapplication: **последнее** вхождение опции побеждает (код FPC 3.2.2); `GetOptionValues` → выходной массив + count | TCA-17, TCA-07 |
| R11 | math: движок **никогда не умирает** — все деления в прелюдии защищены и возвращают `+inf`/`-inf`/`nan` как FPC; при реальной недоступности движка публичный метод = rc 1 + пусто; `read -t` как страховка от зависания; awk под `LC_ALL=C`; токены `inf`/`nan` нормализуются в `_fe`; программа передаётся через process substitution (если cygwin-проба пройдёт), иначе EXIT-trap | M1–M3, M5, M6, M9, M10, M15 |
| R12 | tregex T4 (anchored zero-length): сейчас **исправить docs S6/§4 + закрепить тестами**; детекцию — только если появится потребитель `\b`-токенизации | T4 |
| R13 | ФС-юниты: листинги **включают** dot-entries (FPC/.NET); рекурсия **не заходит** в dir-symlink; `delete` срезает хвостовые `/` и на симлинке удаляет только ссылку; tfile set*Time/setAttributes реализуются через общие хелперы с tdirectory (creation time → rc 1); добавляется `readAllTextVar NAME` (без форка, с хвостовыми `\n`); copy/move в существующий каталог = содержимое / rc 1 | G6-04, 07, 09, 10, 17, 18 |
| R14 | Мёртвые `source kerr.sh/klib.sh` убираются из tlist/tdictionary/tstopwatch (kerr не адаптируется) | G8-03 |
| R15 | `.ckk`: `**/.ckk` в `kcl/.gitignore` остаётся **и** kklass-тесты 030/063/065 переводятся на `KKLASS_CKK_DIR`/tmp; 4 устаревших каталога `.ckk` под kcl удаляются | G8 task 6 |

## 3. Фазы

Порядок = риск × использование. Каждая фаза: код → тесты → docs юнита → гейт → «go» владельца → коммит.

### P0 — Фундамент (kklass, kkore, инфраструктура тестов) — S
- kklass: `kk._return` → `printf '%s'` (G1-13/G2-03); `kklass.sh:536` `${!__kk_dv:-}` (G1-14); `kklass_pascal.sh:42` гвард под `set -u` (G8-01); `local p sm wm` (G8-04); тесты 030/063/065 → `KKLASS_CKK_DIR` (R15). Регресс-тесты: `$()` значения `-e`/`-n`; `.delete` под `set -u`; `set -u; source kklass_pascal.sh`.
- kkore/klib.sh: `kk.isInt NAME_OR_VALUE` и `kk.isNum` (D1) + тесты kkore.
- Раннеры: `kbool/tests/tests.sh`, `ktests.sh` — `LC_ALL=C.UTF-8` (D6); в шаблон юнита — блок самоподстановки локали.
- `kcl/README.md` (новый): **контракт kcl** — возврат (RESULT/rc), ошибки и debug-переключатель, булевы = rc, `set -eu`, локаль, числовые аргументы, зарезервированные имена выходных массивов, правило «без форков в горячих путях».
- Удалить устаревшие `kcl/**/.ckk`, закоммитить `.gitignore`.
- Гейт: kklass 273+новые, kkore, master 5.2 и 5.3.

**DONE 2026-09-06** (закоммичено по команде владельца после ревью второго круга; push НЕ делался). Красный-первым: 127 (6 FAIL), 128 (18 FAIL), 129 (5 FAIL), kkore 006 (96 FAIL / 0 PASS), ktests 030 (7 FAIL при закомментированном пине) — потом правки, потом зелёное. Гейт: kklass 324/324, kkore 331/331, ktests 308/308, мастер-свип 20 наборов / 3238 тестов / 0 FAIL на 5.2.37 И 5.3.9 (было 3066); kklass 335/335, kkore 334/334, ktests 308/308. Отличия от буквы плана: (1) `set -u` понадобился не в одной строке `.delete`, а ещё в 36 косвенных чтениях метаданных (`${!var}`) по kklass.sh/kklass_pascal.sh/kklass_decl.sh/kklass_serializable.sh плюс `${meth_index[$2]:-}`, `${6:-}`, `${2:-}` — иначе `source <unit>.sh` под `set -u` (D7) недостижим в P1; (2) гварды `${_X_SOURCED:-}` починены также в kkore (5 файлов) и ktests (5 файлов), потому что kklass грузит kkore, а каждый будущий `NNN_Contract.sh` грузит ktests под `set -eu`; (3) к 030/063/065 добавлен **031** — он писал `.ckk` в `$PWD` тем же приёмом (его `rm -f .ckk/\"$TEST_FILE\".sh` из-за буквальных кавычек ничего не удалял), в старых каталогах лежал его `test_031.kk`; (4) слабые ассерты 030/031 («Force ИЛИ Compiling», «runtime ИЛИ No compiled») переписаны на точные; (5) локаль пиньется в `ktests/ktest.sh` — единой точке, которую грузят оба раннера И каждый тест-файл, поэтому одиночный запуск файла ведёт себя как в свипе; (6) `kcl/README.md` помечен как **нормативный** контракт, а не описание текущего состояния. P0 затрагивает четыре сабмодуля (kklass, kkore, ktests, kcl) и родителя.

**Ревью второго круга (владелец, 2026-09-06).** Проверено по дереву, не по отчёту; «сначала красный» подтверждён stash-ем kklass.sh. Найден один реальный пропуск в зоне P0: `kerr.sh:5` — в ветке ПОВТОРНОГО source остался голый `"$1"` (исправлен был только хвостовой `${1:-}` на строке 146). Ветка не гипотетическая: kklass грузит kkore, а tlist/tdictionary/tstopwatch (и tstringlist через tlist) подключают `kerr.sh` ещё раз — то есть под `set -u` эти четыре юнита падали бы и после гвардов P1. Тест 128 промахнулся, потому что грузил каждый файл ровно один раз в свежем шелле; теперь он source-ит каждую точку входа kkore и kklass ДВАЖДЫ, плюс реальную форму «kklass, затем юнит» и путь `source kerr.sh set_trap`. Закрыто красным-первым (3 FAIL). Заодно: (a) `kk.isInt 5 __v` из-за динамической области видимости писал собственную локаль функции и возвращал rc 0 — все локали переименованы в `__kk_*`, а `kk._setOut` отвергает `__kk_*`/`__KK_*` (rc 2); (b) обе функции и `kk._setOut` без аргументов падали на `$1` под `set -u` — `${1:-}`/`${2:-}`; (c) в таблице README tregex исправлен на `TRegEx` / static (остальные 16 строк сверены прогоном по числу static- и инстанс-членов); (d) в 128 добавлен кейс булевого метода под `set -eu` через `if`/`||`/`!` — он был зелёным сразу, это закрепление правила README, а не багфикс; (e) в `kklass/PLAN.md` и `kklass_ledger.json` появилась запись `P6_kcl_review_P0` со ссылкой на источник, иначе следующая сессия по kklass не поймёт происхождения правок. Владелец одобрил отсев вне int64 в `kk.isInt`, удаление `kbool/.ckk` (сделано) при сохранении `kklass/tests/.ckk` и все четыре отклонения первого круга. Полный гейт прогнан заново, потому что `klib.sh` и `kerr.sh` грузит каждый kcl-юнит.

### P1 — Сквозной hardening всех 17 юнитов — M
- X-INJ: `kk.isInt` во всех ~40 точках (tlist/tobjectlist/tstringlist/tarray, tstringhelper, tcustomapplication, dateutils, math, tregex, tfile).
- X-ECHO: `echo` → `printf '%s\n'` во всех echo-юнитах (промежуточно, до D3-конверсии).
- X-SETU/X-SETE: гварды `${_X_SOURCED:-}` (17 юнитов), `if`/`+= 1` в math/tregex (M7, T1).
- X-LOCALS: `local` для `i j arg attr_parts letter _dummy_opts`.
- X-DASH: `--` перед путями в tfile/tdirectory/tinifile (G6-11, T7).
- R14: убрать мёртвые source kerr/klib.
- Тесты: в каждом наборе `NNN_Contract.sh` — `set -eu` смоук + инъекционный индекс + `-n`-значение через `$()`.
- Гейт: master 5.2 и 5.3, все `[FAIL]` через grep, ни один бенч не хуже.

**DONE 2026-09-06** (не закоммичено, ждёт «go»). Гейт: мастер-свип **20 наборов / 3370 тестов / 0 FAIL на 5.2.37 И 5.3.9** (было 3238 после P0, +132). Все 17 наборов несут `NNN_Contract.sh` — 97 ассертов, 0 FAIL. Сделано: гварды `set -eu` во всех 17 юнитах (10 переведены, 7 добавлены с нуля) + 58 необязательных позиционных без `:-` и `BASH_REMATCH[0]` — этих двух классов в отчётах ревью не было, их нашли контракт-тесты; 70 гвардов `kk.isInt`/`kk.isNum` (tstringhelper 20, dateutils 17, tlist 13, math 7, tobjectlist 5, tarray 4, tregex 2, tcustomapplication 1, tfile 1); `echo`→`printf` в шести печатающих юнитах (ни одного `echo` не осталось); X-LOCALS; `--` перед путями; R14. Свои две осечки в механических обходах (настоящий перевод строки внутри формата `printf`; регекс, проглатывавший последующие операторы) пойманы и исправлены — от этого класса теперь защищает проверка целостности исходника в каждом контракт-тесте.

**Уточнение X-SETE (владелец, 2026-09-06, проверено на 5.2.37 и 5.3.9).** Сплошное преобразование 143 `[[ … ]] && …` **отменено**: предпосылка была неверна. bash завершает скрипт только для команды, следующей за **последним** `&&`/`||`; упавший левый операнд из-под `set -e` не роняет. Идиома debug-лога `[[ "${VERBOSE_KKLASS:-}" == "debug" ]] && echo … >&2` перед `return 1` благополучно доходит до `return 1`. Живая проверка: `set -e; TList.new L; L.Get 5 || echo reached` печатает `reached` на обеих версиях. Рвут `set -e` только две формы, обе названные планом изначально:
1. **Хвостовой список** — последняя инструкция функции. Скан всех 17 юнитов дал 3 попадания, и все три — намеренные rc-предикаты (`dateutils._two`/`_three`, `TSet._isSet`), у которых **каждое** место вызова уже под `|| return 1` или `if !`. Правок не потребовалось.
2. **Голая команда, у которой rc и есть ответ.** Найдено контракт-тестами, а не чтением кода: компаратор в `TArray._cmp3` и в цикле слияния `sort`, `TStringList._sortcmp`, голый `[[ =~ ]]` в `TRegEx._match1`. Все четыре теперь захватывают статус через `|| rc=$?`.
Плюс голая арифметика: `(( x++ ))` даёт 1 при нуле, `(( n -= 1 ))` — при результате 0, поэтому `+= 1` из плана лечит только половину; единая идиома `(( … )) || :` на 30 мест, записана в контракт `README.md` §1.4. Вместо обхода по коду в каждый контракт-тест добавлен прямой ассерт «падающий член возвращает управление вызывающему под `set -e`» — он и поймает хвостовой список, если тот где-то появится. Дедупликация 66 строк debug-лога через `kk.debug` отнесена в **P9**.

### P2 — Контейнеры: tlist / tobjectlist / tstringlist / tarray + thashset / tqueuestack — L
- G1-01 деструктор TList (`unset ${inst}_items`), `inherited` в TObjectList.Destroy; G1-03/04/05 TStringList (Duplicates только при Sorted, `_setSorted` сортирует, IndexOf = Find при Sorted, `_cmpCore` без диспетчера, dupIgnore возвращает индекс); G1-06 R1; G1-07 overrides `_setCount`/`_setCapacity`/Assign в TObjectList; G1-09 R2; G1-10 Assign копирует флаги; G1-11 без форков (`${src}_data[count]`); G1-12 валидация компаратора TArray; G1-15 кламп binarySearch; G1-16 булевы токены; G1-17/18 тест 015, Clear.
- thashset: G2-01 (**одинарные кавычки в unset**, тесты Remove/Extract на экзотических элементах), G2-02 ToArray, G2-04 R5, G2-05 ForEach; правка PLAN/ledger thashset («verbatim»). Затем thashset продолжает свой роадмап (P2 set algebra → P4).
- tqueuestack: G2-02 ToArray; R3.
- tdictionary: G2-07 docs, R4 предупреждение.
- Перф-гейт: TStringList 300 Add < 10× TList; docs/TStringList.md и docs/TList.md урезаются до портированного подмножества (G1-19), Names/Values/Text/CommaText — в роадмап.
- Гейт: наборы tlist/tobjectlist/tstringlist/tarray/thashset/tqueuestack/tdictionary + master ×2 + бенчи.

**DONE 2026-09-07** (не закоммичено, ждёт «go»). Гейт: наборы фазы 0 FAIL на 5.2.37 И 5.3.9 — tlist 133 (было 121), tobjectlist 52 (38), tstringlist 192 (169), tarray 98 (88), thashset 45 (30), tqueuestack 96 (83), tdictionary 143 (137); мастер-свип 20 наборов / 3460 тестов / 0 FAIL на обеих версиях (было 3370 после P1), все строки `[FAIL]` через grep. Перф-гейт G1-03 выполнен с запасом: 300 Add на TStringList 16626 мс → 174 мс при 122→155 мс у TList (было 136×, стало 1.1× несортированный и 3.0× сортированный); 20 IndexOf по сортированному списку из 300 элементов 824 мс → 34 мс (Find 32 мс). Бенчи tarray и tqueuestack прогнаны до и после — все метрики в пределах шума прогона.

Закрыто (каждая находка — тест, красный до правки): G1-01 (деструктор TList + `inherited` в TObjectList.Destroy), G1-03/04/05 (Duplicates только при Sorted, `sorted` — свойство с сортирующим сеттером, IndexOf = бинарный поиск при Sorted, общий плоский `_findPos` без диспетчера, dupIgnore возвращает индекс через `kk._return`), G1-06 (R1), G1-07a/c (override `_setCount` в TObjectList; TList.Assign падает ДО Clear), G1-09 (R2), G1-10/11 (Assign копирует три флага и не форкает; операнд проверяется по классу), G1-12/15 (tarray), G1-16 (булевы токены TObjectList), G1-17 (мёртвый тест 015 переписан), G1-19 (README для tlist и tstringlist + шапки «портировано / не портировано» в docs/TList.md и docs/TStringList.md, Names/Values/Text/CommaText — в roadmap-таблицу), G2-01/02/04/05/06 (thashset), G2-02 + G2-10/R3 (tqueuestack), G2-07 + R4 + R3 (tdictionary — только docs и тесты-пины).

Отличия от буквы плана (каждое с причиной):
1. **G1-07b «override `_setCapacity`» не сделан** — не нужен: R2 в `TList._setCapacity` (`capacity < count` или `< 0` → rc 1, без изменений) делает потерю элемента на этом пути невозможной, так что переопределять в наследнике нечего. Так же сформулирован и сам fix в отчёте G1 («make `_setCapacity < count` an error … also fixes TList parity»).
2. **G1-07 «override Assign» не сделан** — по той же причине: `TList.Assign` теперь падает ДО `$this.Clear`, то есть владеющий список ничего не освобождает; override, делегирующий в падающую заглушку, добавил бы уровень диспетчеризации без поведения. FPC тоже не переопределяет Assign в TObjectList.
3. **G1-12 закрыт наполовину ещё в P1**: `sort`/`binarySearch` берут start/count после слота компаратора, поэтому гвард `kk.isInt` из D1 уже отвергал опечатку (rc 2, массив не тронут) — проверено. В P2 закрыт остаток: у скан-семейства (indexOf/firstIndexOf/lastIndexOf/contains) после компаратора нет ничего, и токен молча игнорировался; теперь rc 2. `min`/`max` НЕ отвергают: следующий позиционный у них — свободное значение default, поэтому неоднозначность (default, совпавший с именем функции, читается как компаратор) задокументирована в README и закреплена тестом.
4. **G1-18 — премисса находки неверна**: `TList.Clear` не вызывает `_setCount 0` и не делает O(n) `unset`; `$__inst__.property count = 0` — это ПРЯМАЯ запись хранимого значения (`kk._property`), диспетчер свойства обходится (так же документировано в шапке tlist.sh). Кода не менял, добавил тест на результат Clear (count/capacity 0, массив реально пуст, список пригоден дальше).
5. **G2-10/R3 не потребовал правок кода**: и `TObjectQueue`/`TObjectStack`, и `TObjectDictionary` уже создают инстанс с умолчаниями и возвращают rc 1; расхождение из отчёта — это разные УМОЛЧАНИЯ классов (очередь владеет по FPC, словарь без владения по FPC), а не разное поведение при отказе. Закреплено тестами в обоих юнитах и записано как правило дома в README.
6. **Найдено и исправлено сверх списка (в зоне фазы)**: (а) `TStringList.CompareStrings` вызывал `_cmpCore` голой командой — под `set -e` любой ответ «равно»/«больше» убивал скрипт вызывающего (плюс внутренние ошибки bash `pop_var_context`); это тот же класс, что 4 случая X-SETE из P1, закрыт тем же приёмом `|| rc=$?` и ассертом в 017_Contract.sh; (б) `TQueue.ToArray`/`TStack.ToArray` — однострочные обёртки над помощником, а kklass дописывает `kk._return "$RESULT"` в конец каждого `func`, поэтому rc валидации (G2-02) съедался и наружу шёл 0; обёртки переписаны на `_toArray "$1" || return 1; return 0`.
7. **docs/*.md не урезаны, а получили шапку** «upstream-справочник, портировано: …» с таблицей соответствия — вариант, прямо разрешённый формулировкой P9 и согласованный с §2 `kcl/README.md` («docs/*.md — upstream-материал»); удалять исходный дамп DocWiki значило бы терять справочник по членам, которые ещё предстоит портировать.
8. **Решение владельца по ревью P2 (2026-09-07): rc отвергнутого имени выходного массива — 2, а не 1** (побеждает контракт `kcl/README.md` §1.2/§1.7: неверное имя — это неверный ВЫЗОВ, а не значение, которое вызывающий имеет право попробовать). Применено в `tqueuestack.ToArray`, `thashset.ToArray` и `tdictionary.KeysToArray/ValuesToArray/ToArrays` (все три раньше отвечали rc 1). Сначала красный: тесты переписаны на rc 2 и упали — tqueuestack 009 8/13, thashset 004 4/11, tdictionary 009 4/12. Побочно закрыта **дыра того же класса G2-02 в tdictionary**, которую отчёт G2 считал закрытой («tdictionary validates»): старая проверка `[[ -z ]] || ! declare -n ref="$name"` принимала любое зарезервированное имя, и `d.KeysToArray __td_items` привязывал выходной nameref к самому хранилищу — сброс `ref=()` ОЧИЩАЛ словарь и возвращал rc 0 (репро и красное свидетельство в `kcl_ledger.json`, `found_in_P2/P2-F2`). Заодно выяснилось, что обёртки `TQueue.ToArray`/`TStack.ToArray` схлопывали rc помощника в 1 — теперь пробрасывают его дословно.
9. **Тесты, закреплявшие не-FPC диалект, переписаны** (правило §4): tstringlist 012 (dupIgnore на НЕсортированном списке требовал выброса дубля — теперь проверяются обе половины правила FPC), 010 («Invalid capacity assignment» был безусловным `kt_test_pass` — теперь rc 1 + неизменное состояние), 015 (мёртвый grep по синтаксису `method Assign '{`, которого в файле нет — теперь читается реальное тело Assign/AddStrings и проверяется отсутствие `eval` и подоболочек).

### P3 — Файловая система: tfile / tpath / tdirectory — L
- D5 (разделитель `/`, парсеры принимают оба: G6-01/12/15/22); G6-02 changeExtension по имени файла; G6-03 printf; G6-04/17 symlink (R13); G6-05 createSymLink через `cygpath -w` + рабочий probe в тестах; G6-06; G6-07 сеттеры tfile (R13); G6-08 `touch -d @epoch`; G6-09 dotglob; G6-10; G6-13 `realpath -m --`; G6-14 FollowLink; G6-16 форки (`uname`, `ls -A`, вложенные `$()`); G6-18 `readAllTextVar`; G6-19..27.
- D3: конверсия tfile/tpath/tdirectory на `func`+RESULT (R8 для булевых).
- Тесты: tpath 001/003/007 под D5; setter-тесты сравнивают значения; hidden entries; existing dest; symlink dir в delete/recursion; `-`-имена; tpath 012 → `_KT_TMPDIR`.
- Гейт: 3 набора + master ×2.

**DONE 2026-09-07** (не закоммичено, ждёт «go»). Гейт: наборы фазы 0 FAIL на 5.2.37 И 5.3.9 — tpath 243 (было 117), tfile 250 (129), tdirectory 320 (203); мастер-свип 20 наборов / 3827 тестов / 0 FAIL на обеих версиях (было 3460 после P2) (все строки `[FAIL]` через grep). Бенчей у этих трёх юнитов нет; перф-гейт D3/G6-16 закреплён тестами: `tpath.combine` 100 прямых вызовов быстрее 100 через `$( )` (tpath 001), тела 13 членов tpath и 7 членов tdirectory не содержат ни одной подстановки команд (структурная проверка `declare -f`), `uname`/`ls`/`basename` не вызываются (теневые функции-канарейки с файловым следом), а BASHPID при прямом вызове не меняется и RESULT доходит до вызывающего (tpath 019).

Закрыто (каждая находка — тест, красный до правки): G6-01/15/22 (оба разделителя на входе, `/` на выходе, UNC-корень = server+share — tpath 017, 15 FAIL из 24 до правки), G6-02/12/13 (changeExtension по имени файла, getDirectoryName по .NET/ExtractFileDir, `realpath -m` — tpath 018, 12/23), G6-14/16/20/21 (FollowLink, форки, driveExists спрашивает ФС, `~` валиден, nocasematch восстанавливается — tpath 019, 17/32), D3+R8 для tpath (020, 21/34), G6-05 (createSymLink реально создаёт ссылку; 12 «PASS (skipped)» стали настоящими — tfile 040, 6/14), G6-07/08/26 (сеттеры времени и атрибутов tfile настоящие, `touch -d @epoch`, `%W`→`%Y` — tfile 041, 16/24), G6-18/19/26 (`readAllTextVar`, `readAllLines`→массив, replace забирает source, temp-каталог crypt через оба разделителя — tfile 042, 11/18), D3+R8 для tfile (043, 16/22), G6-09/17/24/25 (dot-entries, отказ спускаться в dir-symlink, хвостовой разделитель, failglob, выходной массив — tdirectory 033, 16/18), G6-04/10/16/24/11 (delete симлинка со слэшем, copy/move в существующий приёмник, форки, exists FollowLink, `cd -` — tdirectory 034, 12/27), G6-08/R13 (Utc-сеттеры и creation → rc 1 — tdirectory 035, 12/26), D3+R8+G6-27 для tdirectory (036, 15/29).

Отличия от буквы плана (каждое с причиной):
1. **D3 сделан через `static proc` + `tpath._ret`, а не `static func` + `kk._return`.** Тонкий статический диспетчер kklass (класс без static-переменных) печатает значение `kk._return` **безусловно**, то есть и при прямом вызове, — `static func` echo-ит на каждом вызове (это же измерение записано в шапке `tregex.sh`). Семантика D3 («direct = RESULT без форка, `$()` печатает») достигнута точно; изменений в kklass не потребовалось. Приём записан в `kcl/README.md` §1.1, чтобы P5/P6/P7 не переоткрывали его.
2. **`cygpath -w` + `cmd mklink` для G6-05 не используется**: на этой машине mklink отвечает «недостаточно привилегий» даже с корректными Windows-путями, а `MSYS=winsymlinks:native ln -s` создаёт настоящую ссылку. Отчёт G6 утверждал обратное — проверено обеими командами.
3. **`ln -s` на существующий путь**: `ln -s tgt DIR` кладёт ссылку ВНУТРЬ каталога и рапортует успех, поэтому `createSymLink` сначала требует, чтобы путь ссылки был свободен, — иначе очистка copy-режима удалила бы этот каталог (поймано собственным тестом в 040).
4. **`_KT_TMPDIR` симлинк-проб**: `ln -s` на MSYS по умолчанию в copy-режиме УСПЕШНО делает копию, поэтому проба проверяет `[[ -L ]]` и убирает копию; общий помощник — `tests/symlink_helper.sh` в наборах tfile и tdirectory (не `NNN_*.sh`, раннер его не подхватывает).
4. **Листингам добавлен необязательный 4-й аргумент — имя выходного массива** (§1.7 README): без него имя файла с `\n` неотличимо от двух имён. Старая форма (склейка через `\n` в RESULT) сохранена, ни один существующий вызов не сломан.
5. **`tfile.readAllTextVar FILE NAME`** — имя выходной переменной последним позиционным, как у `str.split` в §1.7 (в тексте R13 порядок не задан).
6. **Сообщения об ошибках tdirectory убраны со stderr** под `VERBOSE_KKLASS=debug` (§1.2). Это не находка G6, а требование контракта: юнит печатал `Error: ...` на каждый промах.
7. **`TDIRECTORY_COLLATE` и подмена `LC_ALL=`/`LC_COLLATE=en_US.UTF-8` в листингах удалены** — порядок берётся из окружения (раннеры пинят `C.UTF-8`, то есть порядок кодовых точек). Ожидания tdirectory 013/014 переписаны под него.
8. **Найдено и исправлено сверх списка**: `cd -- -` в bash 5.2/5.3 всё равно означает `$OLDPWD`, поэтому `--` из P1 (G6-11) не закрыл `setCurrentDirectory -`; член теперь подставляет `./-`. Плюс `tpath.getTempPath` читал `$TMPDIR` без `:-` (падение под `set -u`), а `tpath._statTime` делал лишнюю пробу `[[ -e ]]` перед `stat` — на NTFS это переписывает «старое» время доступа, и getLastAccessTime возвращал «сейчас» вместо только что записанного значения. Ещё один — в моём собственном новом коде, пойман своим же тестом: `ln -s tgt DIR` кладёт ссылку ВНУТРЬ существующего каталога и рапортует успех, поэтому очистка copy-режима удаляла этот каталог; `createSymLink` теперь требует, чтобы путь ссылки был свободен.
9. **Атрибут «только для чтения» берётся из режима, а не из `-w`**: `-w` всегда идёт по симлинку, поэтому оба ответа FollowLink совпадали (это и есть механизм G6-14).
10. **Слабые ассерты переписаны**: tfile 030–036 (`$? -eq 0` на пустом теле → сравнение значений через `stat`), tdirectory 012 (тавтология `-n || -z`), 027/028/029 («непустой ответ» → значения), 022/024 («два чтения времени доступа обязаны совпадать» — на NTFS это не так; теперь оба ответа обязаны быть корректными и различаться не более чем на 2 с), tpath 009 (только «непустой» → полное сравнение), tpath 012 (фикстуры в настоящем `%TEMP%` → `_KT_TMPDIR`), tfile 027/028 (переприсваивание `_KT_TMPDIR`), tfile 027 (фантомный параметр «encoding» → выходной массив).
11. **Мусор миграции в наборе tdirectory** (`# Cleanup\nkt_fixture_teardown` буквальным `\n` в 25 файлах, DEBUG-строки на stderr в 001) убран; там, где вызов teardown был проглочен комментарием, он возвращён.

### P4 — tcustomapplication: порт FPC custapp — M
- D4: FindOptionIndex/GetOptionAtIndex/CheckOptions по FPC 3.2.2 (TCA-01/02/06/09/17/18), ParamCount/Params из сохранённого argv (TCA-03), убрать `"$@"`-инициализацию (TCA-04), EnvironmentVariable валидация (TCA-05), GetOptionValues → массив (R10, TCA-07), LongOpts по `declare -p` (TCA-08), DoRun + Run без форка (TCA-10), Location (TCA-11), EXITCODE не экспортировать (TCA-12), `-`/`--` (TCA-13), TCA-14..20.
- Тесты: переписать ~25 закреплявших диалект; добавить `=value`, `:`/`::`, «needs an argument», регистр, AllErrors, ParamCount на свежем инстансе.
- docs/TCustomApplication.md — сверить с реализацией; README юнита.

**DONE 2026-09-07** (не закоммичено, ждёт «go»). Гейт: набор tcustomapplication **377/377** на 5.2.37 И 5.3.9 (было 327); мастер-свип **20 наборов / 3877 тестов / 0 FAIL** на обеих версиях (было 3827 после P3), все строки `[FAIL]` через grep. Бенча у юнита нет; заявление «без форков» закреплено структурно (см. ниже).

**Эталон.** Локального FPC на машине нет; `packages/fcl-base/src/custapp.pp` (784 строки) взят из `https://gitlab.com/freepascal.org/fpc/source/-/raw/release_3_2_2/packages/fcl-base/src/custapp.pp` (тег `release_3_2_2`) и портирован построчно: `FindOptionIndex`, `GetOptionAtIndex`, `GetOptionValue(s)`, `HasOption`, `CheckOptions` (включая вложенные `FindLongOpt` и `AddToResult`), `GetNonOptions`, `Run`, `Terminate`, `Log`, `GetEnvironmentList`, `GetLocation`, `GetParams`/`GetParamCount`.

Закрыто (каждая находка — тест, красный до правки): TCA-01 (`--opt=value` — единственный синтаксис значения long-опции; имя обрезается по первому `=`), TCA-02 (`:`/`::` в ShortOptions и в имени long-опции, потребление значения, правило «опция со значением последняя в кластере»), TCA-03 (ParamCount/Params из сохранённого argv, индексы ParamStr: `Params[0]` = ExeName), TCA-04 (никакого авто-захвата — argv даёт только `new app "$@"` / `SetArgs "$@"`), TCA-06 (CaseSensitiveOptions действительно читается, с асимметрией FPC: Upper для поиска, Lower для короткой спецификации), TCA-07/R10 (GetOptionValues → выходной массив + count, rc 2 на плохое имя), TCA-08 (массив-vs-строка решается только по самому аргументу LongOpts, через `declare -p` + `${ref@a}`), TCA-09 (AllErrors, склейка через `\n`), TCA-10 (`DoRun` + `Run` = repeat/until без форков и без sleep), TCA-11 (Location от `$0`, а не от `BASH_SOURCE` внутри eval-тела kklass), TCA-12 (EXITCODE — обычный глобал, не экспортируется), TCA-13 (`-` и `--` — невалидные опции по FPC; SetArgs больше не глотает ведущий `--`), TCA-15 (`compgen -e` вместо `env | sort`; значение с `\n` остаётся ОДНОЙ записью), TCA-16 (`printf -v` с аргументами формата, точное вхождение в EventLogFilter, вывод в stderr), TCA-17 (скан вниз — побеждает ПОСЛЕДНЕЕ вхождение), TCA-18 (три ResourceString FPC дословно, с 1-based позицией), TCA-19 (мёртвый код удалён), TCA-20 (`declare -F` перед вызовом OnException). TCA-05 и TCA-14 закрыты в P1 — проверены, не переделывались, но закреплены более широкими тестами (7 неверных имён переменной окружения и 4 неверных индекса Params, каждый с канарейкой).

Отличия от буквы плана (каждое с причиной):
1. **Парсер — восемь обычных функций `tca._*`, а не члены kklass.** Они получают массив данных инстанса по имени, поэтому не стоят диспетчеризации, не могут напечатать в `$( )` вызывающего (ради чего и существовал `kk.call_silent`) и тестируются напрямую. Публичные члены остались тонкими; ответы помощников ходят в четырёх приватных глобалах `__TCA_*`, а не в RESULT.
2. **FindOptionIndex принимает ПАРУ (short, long).** У FPC одно имя на вызов плюс `var LongOpt`, а композицию делают вызывающие; порт носит пару через весь API. Пустое имя = «не задано» и не ищется — FPC ради этого передаёт невозможный символ `#255`.
3. **GetOptionAtIndex сделан публичным** (в FPC он `protected`): после слияния (short, long) вызывающий не знает, какая форма совпала.
4. **GetNonOptions отвечает rc 1** там, где FPC поднимает `EListError` (D2), и не трогает выходной массив. Следствие для вызывающих: строка с одиночным `-`, серединным `--` или неизвестной опцией теперь ПАДАЕТ, а не считает их не-опциями.
5. **GetOptionValues возвращает значения в порядке FPC — ОБРАТНОМ порядку командной строки** (следствие скана вниз, TCA-17/R10). Верно по исходнику и достаточно неожиданно, чтобы про это писали и README, и тест; отмена — две петли.
6. **`CheckOptions` в FPC читает ЗА КОНЦОМ строки кластера.** `O:=O[j]` укорачивает `O` до одного символа, а цикл продолжает индексировать `O[j]` по ИСХОДНОЙ длине; `-cv` со спецификацией `c::v` попадает туда даже без ошибки. Порт сохраняет строку скана целой и запоминает «потребившую» опцию в отдельной переменной — для любого входа, где FPC остаётся в границах, поведение идентично. Это единственное место, где порт намеренно не воспроизводит upstream.
7. **Location — без хвостового разделителя** (FPC `ExtractFilePath` его оставляет) и `.`, если у `$0` нет каталога: как `tpath.getDirectoryName` и весь остальной kcl.
8. **Log пишет в stderr** (у FPC `DoLog` пуст, его переопределяют потомки), **Run отображает ненулевой rc из DoRun на `except HandleException(Self)`**, а базовый `DoRun` ЗАВЕРШАЕТ приложение (пустой, как у FPC, сделал бы `Run` вечным циклом).
9. **Умолчания конструктора оставлены прежними** (`StopOnException=true`, `ExceptionExitCode=1`, `Title='Application'`; у FPC — `False`, `0`, `''`). Ни одна находка их не трогала; записаны в раздел «Differences from FPC» README юнита.
10. **Сверх списка**: юнит получил блок самоподстановки локали `LC_CTYPE=C.UTF-8` (§1.6; `CaseSensitiveOptions=false` складывает регистр через `${s,,}`), а `echo` в `ShowException` и `Log` заменён на `printf` — этот юнит не входил в шестёрку «печатающих», которую подметали в P1.
11. **Отчёт G7 ошибается в одном месте**: заметка A15 «FPC жёстко зашивает `-` в GetOptionAtIndex» неверна — `custapp.pp:397` сравнивает со свойством `OptionChar`. Порт делает так же, тест 023 это закрепляет.
12. **Найдено вне фазы, НЕ исправлено** (`kcl_ledger.json`, `found_in_P4`): (а) **kklass** — чтение ХРАНИМОГО (`var`) свойства под `set -u` роняет вызывающего: `kk._property`/`kk._prop_plain` проверяют `[[ "$4" == "=" ]]` на отсутствующем параметре. Репро: `bash -c 'set -eu; source kcl/tcustomapplication/tcustomapplication.sh; TCustomApplication.new a; a.Terminated'` → `kklass.sh: line 289: $4: unbound variable`. Затрагивает КАЖДЫЙ юнит с `var`-членами, то есть противоречит D7 для всех; правка — по одному `${3:-}`/`${4:-}`. (б) **ktests** — временный каталог файла теста называется по `$0`, а это `bash` у всех файлов, которые запускает раннер, при этом раннер идёт в 8 потоков: все файлы делят `.tmp/bash`, и teardown соседа удаляет его на ходу. Локально обойдено (девять файлов набора делают себе каталог по имени исходника и убирают его за собой).

### P5 — tstringhelper — M
- D3 конверсия (TSH-17; для СТАТИЧЕСКОГО класса это `static proc` + `unit._ret`, а не `static func` — см. отличие 1 в P3 и `kcl/README.md` §1.1) — снимает TSH-02; TSH-01 (P1); TSH-03 split → массив (R9); TSH-04 D6; TSH-05/07/12/13 R9; TSH-06 lastIndexOf; TSH-08 quotedString/deQuotedString FPC без форка; TSH-09 O(n) через `${s//…}`/`${t%%"$v"*}`; TSH-10 (P1); TSH-11 кламп; TSH-14/15/16/18.
- Тесты: 027/034/037 с однозначными ответами; не-ASCII перед искомым; 10 KB перф-гейт; direct-вызовы.
- README юнита (нет).

**DONE 2026-09-07** (не закоммичено, ждёт «go»). Гейт: набор tstringhelper **918/918 на 5.2.37 И 5.3.9** (было 402; +516 ассертов, 3 новых файла), 0 строк `[FAIL]`; мастер-свип запускает ревьюер (мой полный прогон на 5.2.37 до двух последних правок: 20 наборов / 4374 теста / 0 FAIL, «All test suites passed»). Перф-гейт TSH-09 — отдельный файл `066_TSH09_Performance.sh` с относительным бюджетом (20 вызовов на 10 KB против того же объёма работы голым `${big//X/Y}`, плюс абсолютный потолок 1000 мс на 20 вызовов): replace 13 мс, indexOf 252 мс, countChar 11 мс, split 340 мс на 20 вызовов при базовой линии 252 мс; для «плотной» иглы (1024 вхождения) отдельная база `${big//a/}` — countChar 182 мс против 127 мс базы. Отсутствие форков закреплено структурно (`declare -f` по всем 57 членам) и относительным замером «200 прямых вызовов быстрее 200 через `$( )`».

**Эталон.** Локального FPC нет; из зеркала GitLab по тегу `release_3_2_2` взяты и разобраны построчно `rtl/objpas/sysutils/syshelph.inc` (объявление TStringHelper, 680 строк) и `syshelp.inc` (реализация, 1685 строк), а также `sysstr.inc` (Trim/WhiteSpace, AnsiQuotedStr, TryStrToBool, StrToInt/StrToInt64, TextToFloat, IsDelimiter), `syssr.inc` (тело StringReplace), `rtl/inc/sstrings.inc` (InitVal + fpc_Val_SInt_ShortStr — база `$`/`x`/`0x`/`%`/`&`, пропуск пробелов и табов) и `rtl/inc/astrings.inc` (клампы Copy/Delete/Insert, StringOfChar).

Закрыто (каждая находка — тест, красный до правки; в скобках FAIL до правки): TSH-03 (`split STR SEP ARRAY [COUNT] [OPTIONS]` — выходной массив по nameref + count в RESULT, разделитель = одна литеральная СТРОКА, ACount по FPC «лишние части отбрасываются», три TStringSplitOptions; 037 переписан целиком, 35), TSH-04 (блок самоподстановки локали + не-ASCII ПЕРЕД искомым символом в 20 членах; новый 063, 24; 047/050 переписаны, 15+15), TSH-05 (без аргумента флагов — все вхождения, как FPC `Replace(Old,New)=[rfReplaceAll]`; решает КОЛИЧЕСТВО аргументов, а не пустота; 036, 23), TSH-06 (математика StartIndex/ACount по FPC для lastIndexOf и lastIndexOfAny; 027/028 переписаны, 28+24), TSH-07 (FPC TryStrToBool: число → «≠0», иначе TRUE/FALSE без учёта регистра, всё прочее — ОШИБКА конверсии rc 1 + RESULT=''; 041 переписан, 27), TSH-08 (quotedString дублирует любую кавычку и не форкает; deQuotedString с кавычкой-апострофом по умолчанию и toggle IsQuote; 034/011 переписаны, 19+15), TSH-09 (`${s//"$old"/"$new"}`, `${t%%"$v"*}`, `${s//"$c"/}`, один `${t%%"$c"*}` на элемент набора вместо сравнения на символ строки; 066, 10), TSH-11 (клампы FPC Copy/Delete в substring/remove; 040/035, 14+11), TSH-12 (набор пробелов FPC `[#0..' ']`, то есть ≤ #32 и БЕЗ DEL #127; 052, 20), TSH-13 (`indexOf('')` = -1 по FPC 3.2; 017, 3), TSH-14 (грамматика FPC Val для toInteger/toInt64 — пробелы/табы, знак, база `$ x X 0x %` `&`, диапазон назначения; «3.9» и «hello» — rc 1 + RESULT='', а не 0; 046/045/059, 36+13+10), TSH-15 (ошибка printf перехватывается: rc 1 + RESULT='', ни одного номера строки kklass; 015, 7), TSH-16 (все локали трёх nameref-членов с префиксом `__tsh_`, имя приёмника валидируется, зарезервированное = rc 2; 008 переписан, 15), TSH-17 (D3: `static proc` + `string._ret`, прямой вызов молчит, `$( )` печатает один раз, булевы по rc — R8; новый 061, 33; 060 расширен, 7), TSH-18 (toDouble/toExtended/toSingle через kk.isNum после FPC-trim; 043, 58), TSH-19 (алгоритм fphash FPC — `int32((h shl 5) - h) xor c` — с точными значениями в тесте вместо «это целое число»; 016, 9). TSH-01/02/10 закрыты в P1 — проверены (059 расширен до 24 точек инъекции, X-ECHO в 061/060, отсутствие утечек `i`/`arg` в 060), не переделывались.

Отличия от буквы плана (каждое с причиной):
1. **`split` ТРЕБУЕТ имя выходного массива** (rc 2 без него). Старая форма возвращала склейку и была неотличима от входа (это и есть TSH-03); внутри kcl вызывающих `string.*` нет ни одного (проверено grep-ом по всему дереву — только собственные тесты юнита и repro-скрипты ревью), поэтому совместимость сохранять не для кого. У `toCharArray` имя массива, наоборот, НЕОБЯЗАТЕЛЬНО: там старая склейка через `\n` однозначна для ASCII и `$(string.toCharArray abc)` продолжает печатать по символу в строке.
2. **Отрицательный индекс или count в СЕМЕЙСТВЕ ПОИСКА отвергается** (`indexOf`, `indexOfAny`, `indexOfAnyUnquoted`, `lastIndexOf`, `lastIndexOfAny`): rc 1 + RESULT='' через общий гвард `string._nonNeg`. Первая редакция воспроизводила арифметику FPC дословно (`Copy` кламает окно к началу, но к результату прибавляется сырой StartIndex, поэтому `indexOf 'hello world' o -3` = 1, а с явным ACount ответ может попасть на -1, неотличимо от «не найдено») — **отменено ревьюером 2026-09-07**: это неопределённость по недосмотру, а не семантика. Там, где FPC клампинг ДЕЙСТВИТЕЛЬНО определяет (`Copy`/`Delete`, то есть `substring`/`remove`), он сохранён без изменений и закреплён отдельными пинами в 027.
3. **`indexOfAnyUnquoted` сканирует ACount символов, а не ACount-1.** FPC считает предел как `StartIndex+ACount-1` — на один короче, чем у `IndexOfAny`, — из-за чего при умолчании ACount=Length ПОСЛЕДНИЙ символ строки не осматривается вовсе. Это единственное место, где порт намеренно не воспроизводит upstream (как TCA-«чтение за концом строки» в P4).
4. **`lastIndexOfAny` кламает позицию скана к последнему символу**: FPC индексирует `Self[AStartIndex+1]` без проверки границ, то есть читает за концом строки.
5. **`chars` вне строки = rc 1 + RESULT=''**, а не печать слова `undefined` с rc 0 (у FPC `Chars[]` вообще без проверки границ). Слово `undefined` не отличить от данных под `$( )` и оно не является ответом ни FPC, ни контракта §1.2; тест 001 переписан.
6. **Диапазон целых проверяется, а не молча усекается.** FPC `Val` принимает десятичный литерал до Int64 и даёт компилятору усечь его в тип назначения, а не-десятичный — до 2⁶⁴−1 с расширением знака. Здесь не влезающее в назначение значение = rc 1; всё, что влезает, отвечает как FPC, включая расширение знака (`toInteger '$FFFFFFFF'` = −1).
7. **`toBoolean` различает «ложь» и «не конвертируется» через RESULT**: rc 1 + RESULT='false' против rc 1 + RESULT='' (у FPC второе — исключение EConvertError). Оба под `$( )` отличимы, оба закреплены в 041.
8. **`getHashCode` считает по КОДОВЫМ ТОЧКАМ** (TSH-19 — «задокументировать»): алгоритм взят FPC-шный и на ASCII совпадает с ним точно, но на не-ASCII отличается, потому что `fphash` ходит по БАЙТАМ и его ответ зависел бы от кодировки. Заодно исправлена сама формула (`(h+c)*31` без ограничения разрядности → `int32((h shl 5) - h) xor c`) — старый тест 016 проверял только «это целое число».
9. **Сверх списка (в зоне фазы)**: (а) на каждом пути отказа RESULT теперь ОЧИЩАЕТСЯ — `kk.isInt … || return 1` из P1 оставлял в RESULT значение предыдущего вызова, и `string.remove hello ''` отвечал rc 1 при RESULT='hell' (поймано собственным тестом 061/035/040); (б) `string.format` и `string.join` без аргументов падали на `$1` под `set -u` (`local format="$1"; shift`) — теперь `${1:-}` и `shift` под `(( $# > 0 ))`; (в) слабые ассерты, встреченные по дороге: 018 («10 или 13»), 033 («true или True», «содержит 1.23»), 016 («это целое»), 042 (только склейка), 008 (`mktemp` в системном /tmp и один ассерт), 059 (temp-файлы в `/tmp`).
10. **`endsText`/`startsText` следуют TStringHelper, а не StrUtils** (замечание ревьюера 2, красный-первым: 012 — 2 FAIL, 038 — 2 FAIL + 2 переписанных ассерта): пустой subtext = **false** (`TStringHelper.EndsText` = `(ASubText<>'') and …`; у `StrUtils.AnsiEndsText`/`AnsiStartsText` наоборот, а `StartsText` в TStringHelper вообще нет). `startsWith`/`endsWith` — члены TStringHelper со СВОИМ правилом (`Result := L<=0` / `L=0`, пустое значение = **true**); асимметрия принадлежит upstream'у, обе половины закреплены тестами (013, 039).
11. **`compare`/`compareOrdinal`/`compareTo` НЕ приведены к FPC** и записаны в леджер как `found_in_P5`: у FPC это ПРЕФИКСНОЕ сравнение по `min(Length(A),Length(B))` символам (`Compare('abc','ab')` = 0), плюс порядок берётся из коллации локали, а не из кодовых точек. Ни одна находка ревью их не трогала, изменение сломало бы смысл 002/003/005 и выходит за границы фазы.

### P6 — dateutils + tstopwatch — M
- G3-01 нормализация `10#` на публичной границе + `kk.isInt`; G3-02 `_valid_date` в `_parse_iso`; G3-03 `_span_fixed` знак; G3-05 R7; G3-06..10 (валидации, `hh:mm` после часов, компактные ISO-формы, диапазон 1..9999).
- D3 конверсия 185 членов (для СТАТИЧЕСКОГО класса — `static proc` + `unit._ret`, а не `static func`; см. отличие 1 в P3 и `kcl/README.md` §1.1) (R8) — самый большой выигрыш по форкам в kcl.
- Тесты: `08`/`09` во все числовые входы; Feb 30/31; negative MJD; perl-свип ISO-недель как регресс (15k дат, ~40 с) — отдельный медленный тест по флагу; DST-тест с `TZ` если tzdata доступна.
- tstopwatch: G3-11 README (D1 kklass), тест locale-comma.

**DONE 2026-09-08** (не закоммичено, ждёт «go»). Гейт: наборы фазы 0 FAIL на 5.2.37 И 5.3.9 — dateutils **444** (было 131; +313 ассертов, 7 новых файлов), tstopwatch **61** (было 40; +21, 1 новый файл); все строки `[FAIL]` через grep. Мастер-свип — см. таблицу гейта в отчёте.

**Эталон.** Локального FPC нет; из зеркала GitLab по тегу `release_3_2_2` взяты и разобраны построчно `packages/rtl-objpas/src/inc/dateutil.inc` (2985 строк) и `rtl/objpas/sysutils/dati.inc`. Процитированные правила: `IsValidTime` (:535 — 24:00 валидно), `TryEncodeTimeInterval` (:1899 — `Min<60 and Sec<60 and MSec<1000`, четыре `word`), `IncYear` (:1561 — заканчивается `EncodeDateTime`), `DaysBetween` (:1404), `TryISOStrToDate` (:2740 — только длины 8 и 10), `TryISOStrToTime` (:2763 — длины 2/4/5/6/8/10/12 + зона), `TryISOStrToDateTime` (:2849 — разрез по ПОЗИЦИИ 11 или 9), `TryISOTZStrToTZOffset` (:2876), `TryISO8601ToDate` (:2915), `ScanDateTime`/`intscandate` (:2496 объявление `lasttoken`, :2505 правило «M после H = минуты», :2615-2618 прозрачный `:`, :2640 присваивание `lasttoken`), а также `SysUtils.TryEncodeTime` (`dati.inc:117` — `Hour<24`).

Закрыто (каждая находка — тест, красный до правки; в скобках FAIL до правки): G3-01+G3-04 (`018_G3_NumericBoundary.sh`, 28 — гвард на ВСЕХ 185 членах: инъекционная канарейка по всему интерфейсу в трёх позициях, четыре формы мусора (`''`, `abc`, `x[$(…)]`, `1.5`) с проверкой «rc 1 + RESULT='' + ни байта вывода», 48 пинов на `08`/`09`; проверка полноты — список исключений против интерфейса), G3-02+G3-09 (`019_G3_IsoForms.sh`, 30 — ISO-слой переписан четырьмя помощниками по FPC: `_tz_offset_east`, `_iso_date`, `_iso_time`, `_iso_datetime`, плюс `_parse_iso` = FPC-снятие зоны + fallback на date-only), G3-03+G3-06+G3-07+G3-10 (`020_G3_JulianAndRanges.sh`, 32 — знак в `_span_fixed`, `_jd_to_kdt` с проброшенным rc, `_time_interval` по FPC `word`, диапазон 1..9999 у `incYear`/`incMonth` и `_encodable_time` у `encode*`), G3-08 (`021_G3_ScanDateTime.sh`, 8 — `lasttoken`, прозрачный `:`, `Shhmmerror`, незакрытая кавычка), G3-05/R7 (`022_G3_LocalOffset.sh`, 7 — `_local_offset_ms [EPOCH]`, дата-зависимый offset во всех шести конверсиях), D3+R8+D6 (`023_D3_ReturnContract.sh`, 75 — `static proc` + `dateutils._ret`/`_retBool` на всех 185 членах, 26 булевых по rc, блок самоподстановки локали), G3-11+G3-12 (`tstopwatch/006_G3_BooleansAndLocale.sh`, 14 — rc-булевы, отсутствие форка у свойства, три локали). `024_IsoWeekSweep.sh` — не находка, а закрытие пробела покрытия: свип ревьюера превращён в регресс-тест (по умолчанию каждый 37-й день 1900..2100, при `KCL_SLOW_TESTS=1` — все 73 414 дней); зелёный сразу, 0 расхождений с perl.

Отличия от буквы плана (каждое с причиной):
1. **Отчёт G3 ошибается в G3-09**: `tryISOStrToDateTime 2011`, `20110326` и `T19:15` в FPC НЕ валидны — `TryISOStrToDateTime` (`dateutil.inc:2849`) режет строку по фиксированной ПОЗИЦИИ (символ 11, затем 9 должен быть `' '` или `'T'`), поэтому голая дата и голое время датой-временем не являются. Реализованы формы, которые FPC действительно принимает (`20110326T19:15`, `20110326 19:15`, `19`, `1915`, `191530.555`, …), остальное задокументировано в README таблицей форм. FPC побеждает отчёт (правило брифа).
2. **Незакрытая кавычка в `scanDateTime` отвергается вопреки FPC** (план требует именно этого): FPC уходит за конец шаблона и возвращает значение. Записано в README и в шапке теста.
3. **Час 24**: `encodeTime`/`encodeDateTime`/`recode*` переведены на `SysUtils.TryEncodeTime` (`Hour<24`), а `isValidTime` остался на `DateUtils.IsValidTime` (24:00 валидно) — асимметрия принадлежит самому FPC и заодно закрывает «год 10000» из G3-10. Тесты 003 и 015, закреплявшие не-FPC диалект («FPC quirk: 24:00 → 86400000», «FPC allows ms == 1000»), переписаны по правилу §4.
4. **Свойство `isRunning` не может нести rc** — kklass дописывает `kk._return "$RESULT"` в тело shim-метода свойства, и статус схлопывается в 0 ДО `kk._prop_computed` (`kklass.sh:319`, который его пробрасывает). rc несёт func-форма `GetIsRunning`; слово несут обе. Правка была бы в kklass — вне фазы, записано как `found_in_P6`.
5. **Булевы различают «нет» и «это не дата»**: `isValidDate 2011 2 30` = rc 1 + `RESULT='false'`, `isValidDate abc 1 1` = rc 1 + `RESULT=''` (§1.2 против R8). Тот же приём, что у `tstringhelper.toBoolean` в P5.
6. **Перф**: гейт «ни одна метрика не хуже» выполнен НЕ полностью. Валидация каждой публичной границы стоит один `kk.isInt` ≈ 52 мкс, поэтому callee-метрики простых экстракторов выросли на 5–10 %, а `dayOfTheWeek` (не проверявший НИЧЕГО — это и есть G3-04) — вдвое, 63→130 мкс. В обмен: избыточные проверки убраны с внутренних путей (пары `_days_from_civil`/`_days_from_civil_i`, `_floor_day`/`_floor_day_i`; `_split_kdt` публикует `__kdt_day`/`__kdt_dow`), и `weekOfTheYear` стал быстрее на 38 % (634→391 мкс); главный выигрыш D3 — на стороне ВЫЗЫВАЮЩЕГО: 0.19 мс прямого вызова против 19 мс `$( )`, ~100×. В `bench.sh` добавлена секция «caller cost», в README — честная таблица обеих стоимостей вместо старого «≤0.3 ms/call, zero forks» (оно описывало только callee).
7. **`022` переписан на один дочерний shell на зону**: `source` юнита в дочернем bash стоит ~15 с (kklass строит класс из 185 членов), девять детей давали 2 м 36 с на файл; теперь 46 с при тех же 12 ассертах.
8. **Сверх списка, в зоне фазы**: `TStopwatch.Create` печатал свою debug-диагностику через `echo` (сметание X-ECHO/G1-13 в P1 шло по шести «печатающим» юнитам, tstopwatch в них не входил) — заменено на `printf`.
9. **Найдено вне фазы, НЕ исправлено** (`kcl_ledger.json`, `found_in_P6`): (а) kklass — method-backed свойство не может вернуть ненулевой rc (см. отличие 4); (б) осиротевшие процессы прошлых сессий (дерево `bash`+`perl` от 2026-09-06 и дерево убитого по таймауту свипа) удваивали время наборов на этой машине — набор dateutils шёл 9 м 30 с вместо 5 м 34 с; после `kill` дерева время вернулось. Это не дефект кода, но следующей сессии стоит проверять `ps -W` перед замерами.

### P7 — math + tregex — M
- math: R11 (M1/M2/M3/M5/M6/M9/M10/M15), M4 (P1), M8 переполнение → движок, M11 `-0`, M12, M13 randomRange, M14 `_is_num` гейт; D3 конверсия публичных обёрток (для СТАТИЧЕСКОГО класса — `static proc` + `unit._ret`, а не `static func`; см. отличие 1 в P3 и `kcl/README.md` §1.1).
- tregex: T1 (P1), T2 (P1), T3 docs + тест локали, T4 R12, T5 флаги, T6 escape O(n), T7 документировать/оптимизировать скан, T8 nameref-валидация + reserved list, T9/T10 docs.
- Тесты: domain-boundary для каждой Tier-B функции; `frexp inf` с таймаутом; `\n` в аргументе; temp-файл после выхода; `set -e` смоук; anchored `$`/`\b`.

**DONE 2026-09-08** (не закоммичено, ждёт «go»). Гейт: наборы фазы 0 FAIL на 5.2.37 И 5.3.9 — math **177** (было 90; +87 ассертов, 4 новых файла), tregex **246** (было 197; +49, 4 новых файла); мастер-свип — см. таблицу гейта в отчёте. Бенчи прогнаны до и после на одинаковой нагрузке (стенд шумит ±10 %, поэтому базовая линия снималась заново прямо перед сравнением).

**Эталон.** Локального FPC нет; `rtl/objpas/math.pp` (2811 строк) взят из зеркала GitLab по тегу `release_3_2_2` и разобран построчно. Процитированные правила: `Frexp` (:1118 — цикл, который для ±Inf НЕ завершается), `IntPower` (:1044 — возвращает **float**), `Power` (:1030), `SumInt` (:1224 — Int64-аккумулятор, тихое переполнение), `RandomRange` (:1405 — `Random(Abs(aFrom-aTo))+Min`, верхняя граница исключена), `RoundTo` (:2606 — `Round(AValue/RV)*RV`, банковское округление), `DivMod` (:2463 — отрицательное делимое инвертирует и частное, и остаток), `Sign` (:729 — `ord(v>0)-ord(v<0)`, NaN = 0), `InRange` (:2157 — NaN = false), `EnsureRange` (:2185 — NaN проходит насквозь), `CompareValue` (:2576 — NaN = 1), `SameValue` (:2382), `IsZero` (:2222), `Max` (:2033 — ничьи и неупорядоченные пары дают ВТОРОЙ операнд). Для tregex эталон — не FPC: порт .NET-образный `TRegEx` поверх bash ERE, паритет с задокументированным подмножеством (кросс-проверка FPC `TRegEx` над PCRE2 остаётся в `tests/008`).

Закрыто (каждая находка — тест, красный до правки; в скобках FAIL до правки): M1 (`014_M1_EngineDomain.sh` — файл ПАДАЛ по SIGPIPE на первой строке, с подавленным SIGPIPE 55 FAIL/0 PASS; все 44 деления прелюдии переведены на `_dv()`, 43 доменные строки проверяют значение И живость движка И правильность следующего вызова), M2 (тот же файл — `frexp ±inf|nan|1e308|0` висели по 20 с; `_frexp` ограничен по шагам и спецкейсит нефинитные, потому что FPC-цикл на ±Inf не завершается вовсе), M3+M14 (`015` 15 FAIL — валидация КАЖДОГО аргумента до записи в pipe + отказ от request-строки с `\n`; 20 членов × 9 форм мусора), M5 (нормализация токенов в обе стороны + `cmp` возвращает 2 для неупорядоченной пары, и каждый член следует правилу FPC для неё), M6 (`015` — программа уходит в awk через process substitution, файла нет вовсе; запасной путь с temp-файлом цепляет EXIT-трап вызывающего, а не заменяет его), M8 (`017` 8 FAIL — `_ipow_i` с проверкой переполнения, дальше движок и Double), M9 (`015` — `wait` освобождает слот coproc, предупреждения нет), M10 (`015` — 22 члена под `PATH=` дают rc 1 + пусто + тишину), M11 (`_pn` складывает −0 в +0), M12-вторая-половина (`016` — `${3-0}` вместо `${3:-0}`), M13 (`017` — 63 бита из пяти `$RANDOM` + rejection sampling, полный диапазон достижим), M15 (`015` — `LC_ALL=C awk`), D3+R8 (`016` 6 FAIL — все 123 члена на `static proc` + `math._ret`, шесть предикатов по rc); tregex: T3 (`011` 3 FAIL — docs врали, что движок считает символы под C; блок самоподстановки локали), T4/R12 (`011` — задокументировано и закреплено 13 строками с ответами .NET рядом), T5 (`012` — файл ВИСЕЛ 600 с, потому что `split … IFS` привязывал IFS к массиву; строгий разбор флагов, rc 2), T6 (`013` — 14 подстановок вместо посимвольного цикла, 471→112 мкс, 50 000 символов 14.3 с→0.6 с), T7 (`013` — вторая копия остатка на матч убрана, кривая закреплена), T8 (`012` — валидация имени выходного массива, rc 2, полный reserved-список), T9/T10 (`014` — двузначные `$10`..`$99` по правилу .NET + README).

Отличия от буквы плана (каждое с причиной):
1. **`read -t` не на каждом вызове движка** (буква R11). Замерено на обеих версиях: `read -t` стоит ~410 мкс на вызов (не зависит от величины таймаута — bash в таймерном чтении опрашивает побайтно) против ~105 мкс на весь round-trip, то есть даёт 4.9× регрессии на КАЖДОЙ метрике Tier B. `-t` остался на стартовой пробе и включается на каждый вызов через `__MATH_FE_STRICT`. Случай, ради которого R11 просил страховку — движок УМЕР — таймаута не требует: pipe закрыт, обычный `read` падает сразу; а зависание изнутри (M2) теперь невозможно структурно.
2. **Перф-гейт «ни одна метрика не хуже» выполнен НЕ полностью.** tregex: всё в пределах шума, кроме `isMatch` (+12 мкс) и `match` (+10 мкс) — это цена строгой валидации флага (T5); `escape` быстрее в 4.2 раза. math: callee-метрики выросли на 10–25 % (`intPower` 76→111 мкс — вместо голого `**` появилась настоящая проверка переполнения). Это и есть предмет фазы: валидируется каждый числовой аргумент (M14) и каждый член отвечает через `math._ret` (D3). Плата вызывающего — то, что программа платит на самом деле — упала в **184 раза** (17690 → 96 мкс, потому что прямой вызов больше не требует подоболочки); в `bench.sh` добавлена секция «Caller cost», как в dateutils в P6.
3. **`math.sumInt` при переполнении отвечает Double, а не заворачивается как FPC.** `SumInt` в FPC (`math.pp:1224`) — Int64-аккумулятор с тихим переполнением, и `$(( ))` в bash воспроизводит это точно. План (P7/M8) требует ухода в движок; расхождение записано в README («Differences from FPC») и в леджер, откат — одна строка.
4. **T4 закрыт документацией и пинами, кода сканов не касался** — так и просит R12: восстановить настоящую позицию нулевой длины нельзя, bash смещение матча не отдаёт.
5. **`escape` не стал линейным.** T6 требует «O(n) через 14 подстановок»; подстановки сделаны, но `${var//pat/rep}` в самом bash квадратична на обеих версиях (одна подстановка по 10/20/40/80 КБ: 11/48/183/712 мс). Поэтому гейт в `013` относительный — против заменённого посимвольного цикла (тот держится в тесте как эталон), а не абсолютный.
6. **Найдено вне фазы, НЕ исправлено** (`kcl_ledger.json`, `found_in_P7`): (а) gawk не следует IEEE для NaN — `x != x` ЛОЖНО, а `nan > 1` ИСТИННО (сравнение скаляров идёт по полному порядку), поэтому NaN приходится ловить через `sprintf("%g")`+`index()`; (б) `${var//pat/rep}` квадратична (см. 5) — затрагивает и tstringhelper; (в) `read -t` дорог (см. 1); (г) два простаивающих дерева bash от 2026-09-07 под самим харнессом проверены перед каждым замером (прирост CPU ~0 за 4 с) — это не «убежавшие» деревья из P6, их не трогали.
7. **Сверх списка, в зоне фазы**: (а) переписан слабый ассерт `math/tests/004_RangeCompare.sh` («isZero integer path forked») — он сцеплял `o=$(...) && [[ ... ]]`, что с rc-булевыми по R8 ломается; теперь rc и слово проверяются отдельно; (б) блок самоподстановки локали добавлен и в math (§1.6 требует его от каждого юнита); (в) `math.feStop` теперь ещё и убирает за собой файл запасного пути.

### P8 — tinifile — M
- T1 (`-d`), T2/T3 R6, T4 SectionExists FPC, T5 rc eager, T6 R6(a), T7/T8 persistence (R6), T9/T10/T11/T12/T13, T14 ledger.
- Тесты: каталог/RO/`-`-имя/tmp-очистка; пустые и comment-only секции; `[x`/`y]` и `[;x]`; относительный перф-гейт 800 vs 200 append.

### P9 — Документация и леджеры — M
- README (API-таблица + контракт) для 7 юнитов без него: tcustomapplication tdirectory tfile tlist tpath tstringhelper tstringlist; docs/*.md урезаются до портированного подмножества или получают шапку «справочник upstream, портировано: …».
- Статусы 8 леджеров (G8-09); tstopwatch README (G3-11); tdictionary/thashset drift (G2-06/07); tregex docs (T3/T4); math README (M6/M10).
- `kcl/README.md` дополняется таблицей юнитов и их контрактов.

### P10 — fpjson P0: перепланирование — S
- D8: переписать `fpjson/PLAN.md` — оконный regex-лексер (окно 2 KB, `[[ $w =~ ^(token) ]]`, `LC_ALL=C` в области лексера), явный стек вместо рекурсии, `_kids[id]` как список id, `_parent[id]`, деструктор освобождает все суффиксы, UTF-8 из code point вручную (astral/суррогаты), числа-как-литералы (+ `-0`, `1E400`, int64), политика дубликатов ключей на parse и serialize, max-depth, контракт ошибок = D2, FindPath-грамматика для имён с `.`/`[`, тест teardown 10k узлов.
- Бенч-проба как P0-гейт: 20 KB ≤ 1 с на 5.2 оконным лексером; сравнить с посимвольным.
- Только после этого — реализация по роадмапу fpjson.

## 4. Правило для тестов: сначала красный

Каждая находка из `REVIEW.md` получает регрессионный тест с её ID в имени или в `kt_test_start`. Порядок обязателен:
1. тест пишется **до** правки и запускается на текущем коде — он обязан **упасть** (если не падает, тест переписывается, а не правка откладывается);
2. затем правка, тест зеленеет;
3. в леджере у находки фиксируется имя теста.

Системно переписываются слабые ассерты (REVIEW.md §3): `$? -eq 0` → сравнение значения; «любой из двух ответов» → один ответ; `*a*`-подстроки → точное равенство; `if ParamCount>0` и подобные гварды убираются; безусловные `kt_test_pass` удаляются; symlink-probe в tfile переводится на существующую цель, чтобы 12 «PASS (skipped)» стали настоящими тестами. В каждый набор добавляется `NNN_Contract.sh`: загрузка под `set -eu`, инъекционный индекс, значение `-n` через `$()`, освобождение всех `${inst}_*` после `.delete`, не-ASCII перед искомым символом там, где есть строки. Перф-стенки закрываются относительными гейтами (например, 800 append < 10× 200 append), а не миллисекундами.

## 5. Гейты (каждая фаза)
1. Набор юнита(ов) фазы — 0 FAIL на bash 5.2.37 и 5.3.9 (последовательно, не параллельно).
2. `kbool/tests/tests.sh` — 0 FAIL на обеих версиях; читать **все** строки `[FAIL]` через grep, не хвост.
3. `bench.sh` юнита (где есть): ни одна метрика не хуже предыдущей фазы; для конверсий D3 — direct-вызов без форка подтверждён (`BASHPID` неизменен).
4. Смоук `set -eu` для затронутых юнитов (с P1 — в каждом наборе).
5. `git status` чистый после свипа; `.ckk`/`/tmp` не растут.
6. Коммит только по «go»; kcl раньше kbool.
7. Для каждой закрытой находки в леджере указан тест, и этот тест был красным до правки (п. 4).

## 6. Оценка объёма
S ≈ до полудня работы, M ≈ 1–2 дня, L ≈ 2–4 дня. Итого ≈ 3–4 недели последовательно; P2/P3/P4/P5 независимы друг от друга после P1 и могут идти в любом порядке по указанию владельца.
