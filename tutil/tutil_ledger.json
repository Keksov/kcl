{
  "schema_version": 1,
  "project": "kcl/tutil + kcl/tgrep — TUtil: CLI-tool wrapper base (FPC TProcess kinship; typed options → argv, rc normalisation, sinks via TPipe); TGrep: the first wrapper over GNU grep 3.0.",
  "created": "2026-09-10",
  "updated": "2026-09-15",
  "status": "P4 (D6 final) DONE 2026-09-15 (kcl <SHA-D6>), reviewer verification pending. TUtil gained `var subshellOk` (default 0; 1 -> every sink passes TPipe's `-s`, i.e. 'the subshell scope is intended'), per the owner's D6-final ruling Q9 of 2026-09-15; TGrep inherits it unchanged. tutil 184/184 (61+64+51+8) and tgrep 185/185 (74+53+49+9) on bash 5.2.37 AND 5.3.9, threaded; tutil also under --mode single. Red-first 13 FAIL across the five changed test files. Master sweep: run by reviewer. Earlier: COMPLETE (P0-P3) 2026-09-11 (kcl b622134). Both units shipped: kcl/tutil (TUtil, the CLI-tool wrapper base, concrete generic argv runner) and kcl/tgrep (TGrep : TUtil, GNU grep 3.0). tutil suite 172/172 and tgrep suite 184/184 on bash 5.2.37 AND 5.3.9, threaded and under --mode single. Benches rc 0 under `bash -eu` on both bashes with every gate PASS: tutil `u.each` 0.97x / 1.00x of TPipe.each called directly on the same argv (gate 1.1x); tgrep `TGrep.search` 1.17x / 1.23x of a bare `grep -r` (gate 1.3x). Master sweep: run by reviewer: 23/23 suites 0 [FAIL] on bash 5.2.37 and 5.3.9 (2026-09-14, after the P3-F1 fix). One code fix landed in this phase, P3-F1 (see execution_log): the tgrep §2.7 `-0` derivation claimed ownership of `nul` whenever the derivation CONDITION held, so a later non-deriving build cleared a `nul = 1` the CALLER had set; found while writing the docs, FIXED in the same phase with a one-line guard in TGrep.buildArgv, red-first 1 FAIL of 184, pinned by tgrep/tests/004_Argv.sh §G and documented in tgrep/README.md §4, tutil/docs/TUtil.md §3.8 and the kcl README §2 row. Earlier: P2 (TGrep) DONE 2026-09-11 (kcl 1a5862b). tgrep suite 174/174 on bash 5.2.37 and 5.3.9 (tutil 164/164, tpipe 171/171 unchanged); master sweep 23/23 suites 0 [FAIL] on both. P1 DONE 2026-09-11 (kcl 956f2b0). tutil suite 164/164 on bash 5.2.37 and 5.3.9 (tpipe 171/171 after the __TPIPE_QUIET seam); master sweep 22/22 suites 0 [FAIL] on both. P0 DONE 2026-09-11 (kcl 8771cab). tutil suite 59/59 on bash 5.2.37 and 5.3.9; master sweep 22/22 suites 0 [FAIL] on both. Earlier: Depends on kcl/tpipe P0–P1. Design accepted by the owner 2026-09-10 (D1–D5 in tpipe/PLAN.md §2.0). Critic pass 2026-09-10: 23 findings (5 BLOCKER, 10 MAJOR, 6 MINOR, 2 NIT), all folded into PLAN.md §1–§7 (record in §8). No code.",
  "phases": [
    {
      "id": "P0",
      "name": "TUtil core",
      "goal": "Concrete generic runner: cmd/args/argv model, buildArgv (virtual by kklass default), addArg/clearArgs/argv, run, mapRc (virtual), lastRc, destructor; constructor assigns every var.",
      "status": "done",
      "tasks": [
        {
          "id": "P0.1",
          "title": "tutil.sh skeleton + Create (assigns EVERY var: cmd, crlf=0, nul=0, _lastRc=-1)/Destroy/buildArgv/addArg/clearArgs/argv/mapRc/lastRc; sinks as stubs answering `kk._return \"__TUTIL_PENDING__\"; return 2`",
          "category": "infra",
          "severity": "high",
          "status": "done",
          "acceptance": "U2/U3 hold incl. `declare -p ${inst}_data` listing every declared var right after new; argv validates the out-name with kk._outName NAME __tu_ __tg_ AND refuses ${inst}_args|_argv|_paths with rc 2 (the tqueuestack helper shape, status converted); cmd='' → rc 2 everywhere and nothing runs (U4); every internal member call is `kk.call_silent \"$__inst__\" NAME …`, never `$this.NAME`."
        },
        {
          "id": "P0.2",
          "title": "run: `command -v -- \"$cmd\"` pre-check (rc 1, _lastRc=127, one debug line, nothing runs), foreground `\"${argv[@]}\" || rc=$?` (never bare), stdout inherited, raw rc into per-instance _lastRc, mapRc via kk.call_silent → member rc",
          "category": "feature",
          "severity": "critical",
          "status": "done",
          "acceptance": "U5: missing cmd → rc 1, lastRc 127, exactly one stderr line under debug and NONE without it (no bash 'command not found'). U7: `od -c < <(u.run)`, `$(u.run)` and `u.run | od -c` byte-identical to the bare tool — no leaked kk._return digits. A failing tool under set -e does not abort the caller (003)."
        },
        {
          "id": "P0.3",
          "title": "tests 001_Core.sh (red-first against the P0.1 skeleton) + README first cut",
          "category": "test",
          "severity": "high",
          "status": "done",
          "acceptance": "Red count recorded; green on 5.2.37 and 5.3.9; sweep 0 [FAIL]."
        }
      ]
    },
    {
      "id": "P1",
      "name": "Sinks via TPipe",
      "goal": "each/toArray/toList/first/count delegate to TPipe with -- \"${argv[@]}\"; nul/crlf → -0/-c; func sinks answer RESULT = count AND rc = mapped via `kk._return n; return m`; lastRc copied from TPipe.lastRc; mapRc not applied when the consumer stopped.",
      "status": "done",
      "tasks": [
        {
          "id": "P1.1",
          "title": "Five sinks per PLAN §2.3 ordering (save RESULT immediately after the TPipe call; kk._return count; return mapped rc) + stub removal + sentinel-absence test",
          "category": "feature",
          "severity": "critical",
          "status": "done",
          "acceptance": "U1/U6/U8; each sink equals TPipe called directly with the same argv; RESULT = count with rc = mapped both direct and under $( ); three-frame nested dispatch (g.each → TPipe.each → r.onLine) keeps r's state; BASHPID zero-fork probe in the callback; no member answers __TUTIL_PENDING__."
        },
        {
          "id": "P1.2",
          "title": "tests 002_Sinks.sh + 003_Contract.sh",
          "category": "test",
          "severity": "high",
          "status": "done",
          "acceptance": "Red-first counts recorded; set -eu path incl. a failing tool; debug switch one line per rc 2 path; sweep 0 [FAIL]."
        }
      ]
    },
    {
      "id": "P2",
      "name": "TGrep",
      "goal": "kcl/tgrep/tgrep.sh: typed grep options → pinned argv shape, rc 2 list (empty pattern, -F/-E, -l/-L, -h/-H, bad or negative maxCount), mapRc 0/1/≥2 with the partial-output rule, derived -0 for -Z with -l/-L, `binary` (-U), paths, `search` = grep -r with ≥1 path and a unique throw-away instance name.",
      "status": "done",
      "tasks": [
        {
          "id": "P2.1",
          "title": "TGrep class; Create calls `parent.constructor grep` explicitly (NEVER `inherited` — it forwards \"$@\" and doubled every path) and assigns all 22 vars; buildArgv override (argv shape PLAN §1.3); mapRc override; paths; destructor (own arrays then inherited); static search with `__tg_s_${BASHPID}_${__TG_SEQ}`",
          "category": "feature",
          "severity": "critical",
          "status": "done",
          "acceptance": "G1–G4 by array comparison without running grep (incl. ${g}_args empty after new g PAT PATH; maxCount via $__KK_INT, -1 rejected); G5–G12 on the fixture tree; search implies -r and refuses zero paths; instance never implies -r; nested search keeps the outer instance alive; -r does not descend a REAL directory symlink (kt_make_symlink); one missing file among good ones → records kept, RESULT = count, rc 1, lastRc 2."
        },
        {
          "id": "P2.2",
          "title": "tests 004_Argv.sh, 005_Search.sh (GNU banner gate first; fixtures via kt_fixture_tmpdir_create, no own EXIT trap), 006_Contract.sh + tgrep/README.md",
          "category": "test",
          "severity": "high",
          "status": "done",
          "acceptance": "Red-first; green both bashes; the README three-forms example (G9, with recursive=1 / search) executed by a test; sweep 0 [FAIL]."
        }
      ]
    },
    {
      "id": "P3",
      "name": "Closeout",
      "goal": "bench, READMEs final, docs/TUtil.md, TEST_COVERAGE_NOTES for both units, kcl README §2 rows, ledger COMPLETE.",
      "status": "done",
      "tasks": [
        {
          "id": "P3.1",
          "title": "tutil/bench.sh (argv fork-free, run on true, each 10k ≤ 1.1× TPipe direct) + tgrep bench (search ≤ 1.3× bare grep on 10k lines), run in --mode single",
          "category": "perf",
          "severity": "medium",
          "status": "done",
          "acceptance": "Numbers on both bashes in the READMEs; rc 0 under bash -eu.",
          "result": "tutil/bench.sh (310 lines): 6 sections - (a) buildArgv / argv NAME cost plus the RUNS-NOTHING proof (cmd pointed at a counting function, 600 builds, counter 0, BASHPID unchanged); (b) run on `true` against bare `true` and `command true`, published not gated; (c) GATED u.each + a no-op function vs TPipe.each called DIRECTLY on the same argv, gate 1.1x; (d) u.toArray vs TPipe.toArray direct, published; (e) u.each + an INSTANCE-member callback, published not gated; (f) zero forks - BASHPID around 12 members and inside the callback and .Add, plus the producer-is-the-only-fork probe. Defaults N=10000 / ND=300, both overridable, clock = TStopwatch.getTimeStamp, all four measured paths warmed on a 5-record producer first (without that the shape that ran first carried the one-off bind cost and the ratio read 1.13x). rc 0 under `bash -eu` on both bashes, gate 1/1 PASS. Measured 2026-09-11, threaded runner idle (5.2.37 / 5.3.9): buildArgv 266.8/295.9 us, argv NAME 779.2/793.0 us, bare `true` 2.3/3.9 us, `command true` 2.9/3.0 us, u.run on true 1026.4/1123.4 us (two kklass dispatches + one command -v, a per-CALL cost); TPipe.each direct 2078/2072 ms (207.8/207.2 us/rec), u.each 2030/2089 ms = 0.97x/1.00x (gate 1.1x); TPipe.toArray direct 1782/1874 ms, u.toArray 1873/1798 ms = 1.05x/0.95x; u.each + r.onLine 4428/4290 ms = 2.18x/2.05x (kklass's instance dispatch, not ours); 0 forks per record. tgrep/bench.sh (332 lines): GNU banner gate first (a non-GNU grep is on PATH here), a mktemp -d corpus removed by an EXIT trap (NL lines in tree/big.txt with every 100th needle + tree/sub/small.txt), 4 sections - (a) the 22-option buildArgv / argv with the same RUNS-NOTHING proof; (b) GATED TGrep.search vs a bare `grep -r`, gate 1.3x, with TGrep.new + .delete measured on its own line as the delta; (c) g.count and the countOnly shortcut vs `grep -c`, plus a DENSE run, published; (d) zero forks over 13 members plus `search`. Defaults NL=10000 / NR=20 / ND=300. WORKER DEVIATION, accepted and documented in the file header: every (b)/(c) number is ONE process start plus a scan (~27 ms) and one start in twenty takes ~200 ms on this box, so timing the shapes in separate blocks put the drift on one side and the search ratio read 1.07x, 1.23x, 1.46x and 1.51x across consecutive runs of the same code; the shapes are now INTERLEAVED one per iteration and the published ratio is the MEDIAN, with the mean printed next to it - after that it reads 1.17-1.23x on every run. rc 0 under `bash -eu` on both bashes, gate 1/1 PASS. Measured 2026-09-11 (5.2.37 / 5.3.9): buildArgv 895.5/909.6 us, argv NAME 1508.2/1637.0 us; bare `grep -r` median 28.65/26.97 ms, TGrep.search median 33.77/33.30 ms = 1.17x/1.23x (gate 1.3x); TGrep.new + .delete 2547.8/2410.4 us; bare `grep -c -r` median 27.91/26.81 ms, g.count (101 records) 41.05/40.41 ms = 1.47x/1.50x, countOnly+toArray (2 records) 30.70/29.60 ms = 1.09x/1.10x, g.count DENSE (10002 records) 1534.84/1525.05 ms = 54.97x/56.87x; 1 fork per call (grep). tests/004_Bench.sh (8 cases) and tests/007_Bench.sh (9 cases) assert the same shapes with ceilings of 5x (tutil) and 10x (tgrep) plus zero-fork assertions for every member; 007 also asserts TGrep.new + .delete under 50 ms and uses interleaved medians. The 10x tgrep ceiling is a second accepted deviation from '>= 3x the measured ratio': under the threaded runner the two sides do NOT inflate together (grep is its own process and roughly doubles; the wrapper's share is bash work in the contended shell), and a first threaded run measured 4.18x where the next measured 1.16x - the header records both numbers."
        },
        {
          "id": "P3.2",
          "title": "Docs (CRLF measurement table, binary-file note, partial-output rule, reserved member names) + kcl README rows naming BOTH deviations (run streams; rc 1 with real RESULT on partial grep failure) + ledger COMPLETE with SHAs",
          "category": "docs",
          "severity": "medium",
          "status": "done",
          "acceptance": "kcl README §2 rows for tutil and tgrep present; final master sweep 0 [FAIL] on both bashes.",
          "result": "tutil/docs/TUtil.md NEW (the design record, in the house ported/roadmap/wontfix header shape but stating 'no upstream source' like tpipe's): §1 the TProcess comparison member by member against FPC 3.2.2 process.pp + processbody.inc at release_3_2_2 (fetched, quoted) - 11 rows of what maps (Executable/Parameters/Execute/ExitStatus/ExitCode/Output/poUsePipes/poStderrToOutPut/RunCommand/RunCommandIndir/EProcess), 13 rows of wontfix with the reason (the handle-and-lifecycle family, Input/Stderr streams, CurrentDirectory, Environment and why an assignment prefix cannot be an argv word, the Windows console and GUI properties, CommandLine, InheritHandles/PipeBufferSize, the polling RunCommandLoop family, BSD dialects), and §1.3 the two members TProcess does not have (buildArgv, argv); §2 the critic's 23 findings in a table with where each landed AND which test pins it; §3 eight runnable probes with their output on both bashes - $this.func printing 2/0 around the stream, `inherited` in a constructor (cmd=needle, paths in both arrays), the unassigned var (rc 0 + empty on 5.2.37 vs `kklass.sh: line 375: forgotten: unbound variable` on 5.3.9 - the probe that DIFFERS between the bashes) plus .new over a live instance keeping LEFTOVER, `RESULT=3; return 1` handing back the caller's own RESULT, bash's unconditional `command not found` attributed to kklass.sh:376, the __TPIPE_QUIET seam ($(wrapper) = 22 without it, and the >/dev/null 'fix' eating the callback's own bytes), the CR compound-assignment matrix (scalar 3 / array word 2 / declare -a 2 / TAB unaffected / no shopt igncr), and the -Z framing od -c table plus the P3-F1 derivation trace; §4 the §7 wrapper template for tsed/tawk/tfind/thead/ttail with the verified-version notes and the sed `--` asymmetry explained. tutil/README.md rewritten: COMPLETE header with per-bash counts and links to the docs and the coverage notes, the member table (cmd as a function/builtin, the plain-var-prints rule), the return-contract table incl. the partial-output deviation and the pre-check, the run-streams deviation, the three forms shown with TGrep as a RUNNABLE example, the CRLF measurement table (which tools keep the CR and which have already eaten it), __TPIPE_QUIET with the redirect counter-example, the $( ) caveat in both places, §5 'Writing a descendant' with all 17 member-body traps of PLAN §6 plus the two that bite tests, each with a one-line repro, §6 Performance with the two-bash bench table and the small-N caveat, §7 the per-file test table 61/56/47/8 = 172. tgrep/README.md: COMPLETE header with per-bash counts and the P3-F1 note, §4 rewritten to state the derivation limit honestly (the P2 claim 'a caller-set nul is never touched' was too strong) with the failing sequence and the one-line guard, §8 extended with the three buildArgv-specific rules and the CR trap, §9 NEW Performance with the two-bash table and the medians rationale, §10 the per-file test table 74/53/48/9 = 184. TEST_COVERAGE_NOTES.md NEW for both units: every case in per-file tables with an ID, the members, a class (contract / bash-convention / argv / tool-behaviour / representation / boundary / cross-check / perf) and the basis (U1-U8, G1-G12, C1-C23, D2/D4/D6, P0-Fn/P1-Fn/P2-Fn/P3-Fn, §n), plus the deliberate gaps and, for tgrep, the P3-F1 row and its red-first count. kcl/README.md: two rows added alphabetically - tgrep between tfile and thashset, tutil between tstringlist and fpjson - each naming the deviations verbatim as the brief specifies (run streams; rc 1 with the count kept; for tgrep also the partial-failure rule and `search` implying -r with >= 1 path), and the unit-count sentence bumped Eighteen -> Twenty."
        }
      ]
    },
    {
      "id": "P4",
      "name": "D6 final — `var subshellOk` (owner ruling 2026-09-15, Q9)",
      "goal": "Give TUtil the object-style spelling of TPipe's new `-s` flag, so a wrapper instance can declare once that the subshell scope is intended, and pin the D6-final warning behaviour as it reaches a caller THROUGH a wrapper.",
      "status": "done",
      "tasks": [
        {
          "id": "P4.1",
          "title": "var subshellOk declared after _lastRc and assigned 0 in Create; tutil._prep appends -s to __tu_fl when it is 1; TGrep inherits it and does NOT redeclare it",
          "category": "feature",
          "severity": "high",
          "status": "done",
          "acceptance": "`subshellOk` is present in ${inst}_data with 0 right after `new` (TUtil and TGrep); `subshellOk = 1` silences every sink of that instance; the property is read PER CALL, so setting it back to 0 re-arms the warning; KK_SUBSHELL_OK=1 reaches TPipe through the wrapper with no TUtil code.",
          "result": "tutil.sh 650 -> 674 lines: one `var subshellOk` in the class block, `subshellOk=0` in TUtil.Create, and one `[[ \"$subshellOk\" == 1 ]] && __tu_fl+=( -s )` guard in tutil._prep next to the nul/crlf ones (compared as a STRING, never `(( ))`). Nothing else changed. `grep -n subshellOk kcl/tgrep/tgrep.sh` is empty — TGrep inherits it, as PLAN §1.2 requires of every ancestor member name."
        },
        {
          "id": "P4.2",
          "title": "Tests: tutil 002/003 warning matrix through the wrapper, 001 var list, tgrep 004 default and 006 one subshellOk case; READMEs, coverage notes, ledger",
          "category": "test",
          "severity": "high",
          "status": "done",
          "acceptance": "tutil 001-004 and tgrep 004-007 green on both bashes; the warning line asserted with MEMBER = the TPipe member.",
          "result": "tutil 001 (+1 var in the _data row and in the set -u probe, 17-member surface), 002 §B +8 cases (the two warning lines verbatim, subshellOk on/off, KK_SUBSHELL_OK through the wrapper, quiet, count/first silent, the callback-kind rule), 003 §3 the single P1 'D6 warns for count' case replaced by 5 (count/first never warn switch on or off; toArray warns ONCE with the switch OFF and the SAME line under debug; the three silencers; a direct call silent), tgrep 004 §A (subshellOk in TU_VARS and in the boolean-defaults loop), tgrep 006 +1 expect_clean case asserting both directions. tutil 184/184 and tgrep 185/185 on 5.2.37 and 5.3.9; tutil also 184/184 under --mode single. Docs: tutil README member table + flag table + reserved names + a new '§3 subshellOk' subsection, tgrep README inherited-surface paragraph + reserved names, both TEST_COVERAGE_NOTES."
        }
      ]
    }
  ],
  "execution_log": [
    {
      "date": "2026-09-15",
      "batch": "P4 — D6 final: var subshellOk (same worker cycle as tpipe P3)",
      "summary": "Opus worker, reviewer verification pending. Owner ruling D6 final Q9: TUtil gets `var subshellOk` (default 0); `1` makes every sink pass TPipe's new `-s` flag, i.e. 'the subshell scope is intended', which silences TPipe's subshell warning for that instance's sinks. THREE LINES OF CODE: the `var` in the class block, `subshellOk=0` in Create (every declared var is assigned — an unassigned one is unbound under set -u and can inherit a previous instance's value), and one string-compared guard in tutil._prep that appends `-s` to `__tu_fl` next to nul->-0 and crlf->-c. The dynamically scoped KK_SUBSHELL_OK=1 needs NO TUtil code at all and was verified to reach TPipe through both TUtil and TGrep frames, so the property is purely the object-style spelling of the same switch. TGrep does not redeclare it (a descendant `var` with an ancestor member's name is silently overridden by the method wrapper, kklass.sh:911). WHICH CALLS WARN is TPipe's rule, unchanged by the wrapper: `$(u.toArray NAME)` and `$(u.toList INST)` do, `$(u.each r.onLine)` does, `$(u.each fn)` / `$(u.count)` / `$(u.first)` do not; the line names the TPipe member (`Warning: TPipe.toArray: the array BW is filled inside a subshell (BASH_SUBSHELL=1) — ...`), not the TUtil one, because that is where the flag and the variable live. FILES: tutil.sh 650->674, tests/001_Core.sh (+1 var in the _data row, the set -u probe and the 17-member surface), tests/002_Sinks.sh (+8 cases in §B, a p4q producer whose own stderr is discarded so a stderr assertion is not polluted by a broken pipe), tests/003_Contract.sh (§3: the one P1 'D6 warns for count' case replaced by 5 correct ones), README.md (member table, the property->flag table, reserved names, a new subshellOk subsection with the verbatim line and the three silencers, the suite count 172->184), TEST_COVERAGE_NOTES.md; tgrep/tests/004_Argv.sh and 006_Contract.sh, tgrep/README.md, tgrep/TEST_COVERAGE_NOTES.md. RED-FIRST against a scratch copy of the pristine units in a shadow tree (no git stash): tutil 001 3 FAIL, tutil 002 4 FAIL, tutil 003 3 FAIL, tgrep 004 2 FAIL, tgrep 006 1 FAIL. GREEN both bashes, threaded: tutil 184/184 (61+64+51+8), tgrep 185/185 (74+53+49+9); tutil also 184/184 under --mode single on 5.2.37. `bash -n` clean on every touched .sh. Master sweep: run by reviewer. See tpipe_ledger.json P3 for the rest of the ruling (kk.warn, the templates, Q7) and for the one out-of-phase fix this cycle needed (kkore/tests/001_KKLibTests.sh was deleting the whole suite's fixture directory). Committed as kcl <SHA-D6>."
    },
    {
      "date": "2026-09-11",
      "batch": "P3 — closeout (benches, READMEs, docs/TUtil.md, TEST_COVERAGE_NOTES, kcl README rows)",
      "summary": "Opus worker, supervisor review against the live tree. tutil.sh and tests 001-003, 005, 006 UNCHANGED; tgrep.sh and tgrep/tests/004_Argv.sh changed ONLY for the P3-F1 fix below, on the reviewer's instruction after the first report. New: tutil/bench.sh (310), tutil/tests/004_Bench.sh (262), tutil/docs/TUtil.md (670), tutil/TEST_COVERAGE_NOTES.md (260), tgrep/bench.sh (332), tgrep/tests/007_Bench.sh (346), tgrep/TEST_COVERAGE_NOTES.md (420); rewritten: tutil/README.md, tgrep/README.md; edited: kcl/README.md (two §2 rows + Eighteen->Twenty), this ledger, tutil/PLAN.md (status header + a P3 DONE note). Gate: tutil 172/172 and tgrep 184/184 on bash 5.2.37 AND 5.3.9 threaded, and 172/184 again under --mode single on 5.2.37; both benches rc 0 under `bash -eu` on both bashes with every gate PASS; `bash -n` clean on all four new .sh files and on the edited tgrep.sh under both bashes; no stray processes before or after timing (ps -W showed only the harness's own two shells). Numbers are in the P3.1 `result` and in both READMEs. THREE worker findings. (1) P3-F1, found at P3 while writing docs/TUtil.md and FIXED in the same phase on the reviewer's instruction: the §2.7 `-0` derivation set `_nulDerived=1` whenever the derivation CONDITION held, even when `nul` was already 1 because the caller set it, so the next build that stopped deriving took the caller's own `nul` down to 0 with it - the bookkeeping has to tell three states apart (derived = ours, undo it; caller-set = never touch; off) and was only telling two. Repro, identical on both bashes: `g.nul = 1; g.nullOut = 1; g.filesOnly = 1; g.buildArgv` -> nul=1 _nulDerived=1; `g.filesOnly = 0; g.buildArgv` -> nul=0. Fix: one guard inside the deriving branch of TGrep.buildArgv - `if [[ \"$nul\" != 1 ]]; then nul=1; _nulDerived=1; fi` - the un-derive branch unchanged, the comment above it extended. Red-first: the new case tgrep/tests/004_Argv.sh §G '§2.7 (P3-F1): a deriving build does not CLAIM a `nul = 1` the caller already set' failed 1 of 184 against the unguarded code, with the failure line printing exactly `after -lZ: nul='1' _nulDerived='1'; after filesOnly=0: nul='0' _nulDerived='0'`; green 184/184 after the fix on both bashes, and the standalone probe now reads nul=1 _nulDerived=0 through the whole sequence on both bashes while the wrapper's OWN derivation still undoes itself (block 1 of the probe unchanged). The P2 README claim 'a nul = 1 that the CALLER set is never touched' is now true. Documented as found-and-fixed in tgrep/README.md §4 + header, tutil/docs/TUtil.md §3.8 (before/after tables), the kcl README tgrep row and tgrep/TEST_COVERAGE_NOTES.md. (2) A bench that times two shapes in SEPARATE blocks is not measurable on this box when each number is one process start: the tgrep search ratio read 1.07x, 1.19x, 1.23x, 1.46x and 1.51x across consecutive runs of identical code, because one process start in twenty takes ~200 ms. Interleaving the shapes one per iteration and publishing the MEDIAN (mean printed beside it) pinned it at 1.17-1.23x. The same treatment was applied to the three counting shapes. (3) The first measured run of a sink in a process carries a one-off bind cost; without warming all four measured paths first, whichever ran first was penalised and tutil's `each` ratio read 1.13x instead of ~1.00x. Both benches now warm every shape. FPC check: the TProcess comparison was written against the real release_3_2_2 sources (process.pp for the option enums and the RunCommand family, processbody.inc for the class body - the public property block and the published list are quoted from it), not from memory. Master sweep: run by reviewer: 23/23 suites 0 [FAIL] on bash 5.2.37 and 5.3.9 (2026-09-14, after the P3-F1 fix). Committed as kcl b622134."
    },
    {
      "date": "2026-09-11",
      "batch": "P2 — TGrep (kcl/tgrep)",
      "summary": "Opus worker, supervisor review against the live tree. New unit dir kcl/tgrep: tgrep.sh (403 lines) sources ../tutil/tutil.sh; class TGrep : TUtil with the 22 typed vars of PLAN §1.3 plus a private 23rd `_nulDerived`; Create = `parent.constructor grep` (explicit, never `inherited`) + every var assigned + ${inst}_paths; Destroy frees _paths then inherited; paths replaces the operand list; buildArgv override in the PINNED order with the rc 2 list (cmd/pattern empty, -F+-E, -l+-L, -h+-H, maxCount not an int or negative via $__KK_INT without writing back) and the §2.7 derivation (-Z with -l/-L -> nul=1, recorded in _nulDerived so the derivation is UNDONE when the condition stops holding and a caller-set nul is never touched — the brief's set-only rule was not idempotent, worker deviation accepted); mapRc override (0->0, 1->1 silent, >=2->1 + one debug line, 127->1 + debug); static search = `grep -r` on a throw-away instance `__tg_s_${BASHPID}_${__TG_SEQ}`, >=1 path required (rc 2). GNU grep 3.0 under both bashes (msys and cygwin builds); real NTFS symlinks and a newline-in-name fixture were creatable, so no case degraded to SKIP. Red-first against the stubbed skeleton: 143 FAIL of 174 (004: 62/73, 005: 49/53, 006: 32/48). Green: 174/174 on 5.2.37 AND 5.3.9, threaded and --mode single; tutil 164/164 and tpipe 171/171 re-proved. G1–G12 each pinned (see the worker table in the P2 review): argv byte-exact per flag and in combination, -e always, -- iff paths, addArg slot, the four rc 2 pairs, maxCount 08->8 / +5->5 / -1 rc 2, pattern -v/--help/-e/newline as data, no-match rc 1 silent, -E + `(` rc 1 lastRc 2 one line vs literal `(` without -E, -l -Z names with a space and a newline intact while -c -Z stays \\n-framed, directory without recursive rc 1 lastRc 2, search byte-identical to bare grep -r in three positions, the three forms equal under lastpipe (n=2/2/2), partial failure keeps records with rc 1 lastRc 2 (named deviation), nested search keeps the outer instance, -r skips a real directory symlink while -R descends and a command-line symlink is followed, CRLF table (text mode strips CR, binary=1 keeps it), Binary-file record + addArg -a. No TUtil change was needed. Reviewer probes on both bashes: the owner's flagship `TGrep.search needle DIR | TPipe.each r.onLine` == `TPipe.each r.onLine -- TGrep.search …` == `g.each r.onLine` with r's counter at 3 in all three forms, argv `grep -i -r -n -m 8 -e needle -- DIR`, bad ERE rc 1/lastRc 2, search without a path rc 2, CRLF text vs binary, partial failure, delete frees _paths. Master sweep: 23/23 suites 0 [FAIL] on 5.2.37 and 5.3.9. Committed as kcl 1a5862b."
    },
    {
      "date": "2026-09-11",
      "batch": "P1 — the five sinks through TPipe",
      "summary": "Opus worker, supervisor review against the live tree. tutil.sh 374 -> 650 lines: sources ../tpipe/tpipe.sh; tutil._prep MEMBER (kk.call_silent buildArgv -> command -v pre-check -> the __tu_fl flag array from nul/crlf); each/toArray/toList/first/count delegate with -- \"${argv[@]}\" and the ORDERED tail (RESULT saved immediately, TPipe.lastRc -> _lastRc, mapRc via kk.call_silent, kk._return count; return mapped); toArray re-validates the out-name with tutil._badOut (tpipe cannot know __tu_/__tg_ or the family's arrays — without it `u.toArray __tu_v` mapfile'd into the instance's own argv); first answers rc 1 whenever TPipe.first answered non-zero (no record is the answer, producer rc irrelevant; mapRc still runs for its debug line); stubs removed, 001 §G is now the sentinel-ABSENCE section. Red-first against the P0 unit: 77 FAIL of 159 (001: 2, 002: 43, 003: 32) + 4 + 4 for the review changes. Green: 164/164 on 5.2.37 AND 5.3.9, threaded and --mode single. Two worker findings, both cross-unit: (1) tpipe._ret prints under any BASH_SUBSHELL>0, so a wrapper answering through its own kk._return printed the value twice ($(u.count) -> 22) and `u.each cb | cat` carried the count; the worker's first fix (>/dev/null on the TPipe call) swallowed a callback's / .Add's own stdout under $( ) and was replaced on review by a proper seam: tpipe._ret honours a dynamically scoped `__TPIPE_QUIET=1` (declared 0 at load), every tutil sink declares it as a local — kklass's __kk_return_silent cannot serve because the thin static dispatcher sets it to 1 for every static body; tpipe 004 gained 6 cases (171/171), tpipe README §7 documents it. (2) A trailing CR is dropped from a word of a compound array assignment on this platform (`A=( $'cr\\r' )` -> length 2; a scalar keeps it): documented in both READMEs and PLAN §6; tpipe's 003 exotic matrix carried `cr` for that reason (its -c cases use a producer function and are unaffected). Reviewer probes on both bashes: $(u.count) once, each|cat only the callback's bytes, toArray, first-on-true rc 1 / lastRc 0, count on exit 3 keeps the count with rc 1, nul=1 -> NUL records, direct TPipe still prints under $( ). Master sweep: 22/22 suites 0 [FAIL] on 5.2.37 and 5.3.9. Committed as kcl 956f2b0."
    },
    {
      "date": "2026-09-11",
      "batch": "P0 — TUtil core",
      "summary": "Opus worker, supervisor review against the live tree. tutil.sh (374 lines): guard, locale, class TUtil per PLAN §1.2; Create assigns every var (cmd, crlf=0, nul=0, _lastRc=-1) and creates ${inst}_args/${inst}_argv with declare -ga, ctor args verbatim (--format=%H, words with spaces); Destroy unsets both arrays; buildArgv (virtual, cmd empty -> kk._return '' + rc 2); addArg/clearArgs; argv NAME (tutil._badOut = kk._outName NAME __tu_ __tg_ + the three instance arrays, rc 2 before any nameref; COPY of the built argv; runs nothing); run (kk.call_silent buildArgv -> command -v pre-check -> \"${argv[@]}\" || rc=$? -> per-instance _lastRc -> kk.call_silent mapRc -> return RESULT); mapRc base (0->0, 127->1 + debug, else 1 silent); lastRc; the five sinks are stubs (funcs answer __TUTIL_PENDING__ through kk._return, rc 2 + debug). No $this. call anywhere. Red-first against the all-stub skeleton: 47 FAIL of 59. Green: 59/59 on 5.2.37 AND 5.3.9, threaded and --mode single. Reviewer probes on both bashes: $(u.run), od -c < <(u.run) and u.run | od -c byte-identical to the bare tool with -n / a space / a backslash (no leaked kk._return digits); missing cmd rc 1 + lastRc 127 + exactly one debug line; exit 3 -> rc 1 + lastRc 3 silent; two instances keep separate _lastRc; bad out-name rc 2 RESULT ''; delete removes the arrays. Worker deviations accepted: one helper tutil._badOut with rc 0 = refused polarity; func stubs also emit a debug line; argv propagates buildArgv's rc; section G of 001 asserts the stubs (P1 deletes it). Worker fact recorded in PLAN §6: a plain var read at a call site PRINTS and leaves RESULT empty — $(u.crlf) is the only correct read spelling outside a member. Master sweep: 22/22 suites 0 [FAIL] on 5.2.37 and 5.3.9. Committed as kcl 8771cab."
    },
    {
      "date": "2026-09-10",
      "batch": "planning",
      "summary": "PLAN.md + this ledger written after the owner accepted the TPipe/TUtil/TGrep design. TUtil is concrete (generic argv runner, TProcess kinship), buildArgv/mapRc virtual, three per-instance arrays freed by the destructor, all sinks delegate to TPipe. TGrep argv shape pinned (grep [flags] -e PATTERN [extras] -- PATH...), no -P/--color/context. §7 template for tsed/tawk/tfind/thead/ttail. No code."
    },
    {
      "date": "2026-09-10",
      "batch": "critic pass",
      "summary": "Opus critic, 23 probe scripts on both bashes (scratchpad critic_tutil/; base.sh/derived.sh = faithful TUtil/TGrep per the draft). BLOCKERS: (1) `$this.func` inside a member prints the callee's kk._return under $( )/|/<( ) — run leaked '2…0' into its stream; fix = kk.call_silent for every internal member call. (2) `inherited` in a constructor is rewritten to `parent.constructor \"$@\"` (kklass_pascal.sh:166) — TGrep paths doubled; fix = explicit `parent.constructor grep`. (3) an unassigned `var` is an unbound variable under set -u (kklass.sh:363) and .new over a live instance keeps stale _data; fix = constructor assigns every var. (4) G8 and the flagship example contradicted (grep DIR without -r = rc 2, zero records); fix = `search` implies -r and requires ≥1 path, instances never imply -r. (5) `RESULT=n; return 1` in a func loses RESULT (explicit return skips the trailer, kk._invoke restores the caller's); fix = `kk._return n; return m` ordering. MAJORS: `(` is literal under BRE (G6 needs -E); plain `ln -s` makes a directory copy on this box → kt_make_symlink; grep/sed/gawk strip CR in text mode (crlf is a no-op for them) → `binary` (-U) var; a test's own EXIT trap replaces ktests' trap → framework fixtures; -Z is NUL termination only with -l/-L and a var cannot set `nul` → derived -0; grep rc 2 with partial hits and 'Binary file X matches' on stdout → named deviation + escape hatch; bare \"${argv[@]}\" aborts a set -e caller → `|| rc=$?`; fixed throw-away name clobbered by nested search → BASHPID+seq; non-GNU grep on PATH (Embarcadero) → GNU banner gate; missing cmd prints bash's own unconditional stderr → `command -v` pre-check. MINOR/NIT: var-vs-inherited-method same name (method wins silently) → reserved names; kk.isInt accepts negatives and the OUTVAR form writes through the property nameref → $__KK_INT and >= 0; -h/-H and -l/-L pairs rc 2; empty pattern rc 2; sed `--` asymmetry explained; TStringList.Add may answer rc 1 (TPipe guards it); out-name helper status converted to 2; func stubs must kk._return a sentinel. 18 claims verified correct and listed in PLAN §8."
    }
  ]
}
